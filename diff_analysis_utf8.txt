diff --git a/src/app/admin/pos/actions.ts b/src/app/admin/pos/actions.ts
index 9e6efc1..70d199c 100644
--- a/src/app/admin/pos/actions.ts
+++ b/src/app/admin/pos/actions.ts
@@ -1,9 +1,9 @@
 'use server';
 
-import nodemailer from 'nodemailer';
 import { createClient } from '@/lib/supabase/server';
 import { revalidatePath } from 'next/cache';
 import { createClient as createSupabaseClient } from '@supabase/supabase-js';
+import nodemailer from 'nodemailer';
 import { getCurrentCashRegisterAction } from '../caja/actions';
 
 const getAdminClient = () => {
@@ -12,9 +12,6 @@ const getAdminClient = () => {
         process.env.SUPABASE_SERVICE_ROLE_KEY!
     );
 };
-
-import fs from 'fs';
-import path from 'path';
 const backgroundImgBase64 = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAEAAAABACAYAAACqaXHeAAAAGUlEQVR4nO3BMQEAAADCoPVPbQ0PoAAAAAAAAAAA8F8bGgABxZqVdgAAAABJRU5ErkJggg==';
 
 // ÔöÇÔöÇ LOGO REUTILIZABLE (editar solo aqu├¡) ÔöÇÔöÇ
@@ -42,42 +39,14 @@ export async function sendBudgetEmailAction(payload: {
     try {
         const { customerEmail, customerName, items, total, budgetLink } = payload;
         
-        const smtpUser = process.env.SMTP_USER || '';
-        const smtpPassword = process.env.SMTP_PASSWORD || '';
-
-        if (!smtpUser || !smtpPassword) {
-            console.error('Faltan variables SMTP_USER o SMTP_PASSWORD en el entorno.');
-            return { success: false, error: 'Credenciales de correo no configuradas en el servidor.' };
-        }
-
-        const transporter = nodemailer.createTransport({
-            host: 'smtp.gmail.com',
-            port: 465,
-            secure: true,
-            auth: {
-                user: smtpUser,
-                pass: smtpPassword,
-            },
-        });
+        const resendKey = process.env.RESEND_API_KEY;
+        const fallbackEmail = 'contacto@elenalacosturera.cl';
 
         const formatCurrency = (val: number) => {
             return new Intl.NumberFormat('es-CL', { style: 'currency', currency: 'CLP', maximumFractionDigits: 0 }).format(val);
         };
 
-        const attachments = [];
-        let cardBgUrl = '';
-
-        const filePath = path.join(process.cwd(), 'public', 'trabajos', 'model_desnuda_bw.png');
-        if (fs.existsSync(filePath)) {
-            attachments.push({
-                filename: 'model_desnuda_bw.png',
-                path: filePath,
-                cid: 'luxuryPassBg'
-            });
-            cardBgUrl = 'cid:luxuryPassBg';
-        } else {
-            cardBgUrl = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAEAAAABACAYAAACqaXHeAAAAGUlEQVR4nO3BMQEAAADCoPVPbQ0PoAAAAAAAAAAA8F8bGgABxZqVdgAAAABJRU5ErkJggg==';
-        }
+        const cardBgUrl = backgroundImgBase64;
 
         const itemsRowsHtml = items.map((item) => `
             <tr style="border-bottom: 1px solid rgba(245, 242, 235, 0.08);">
@@ -212,19 +181,31 @@ export async function sendBudgetEmailAction(payload: {
 </body>
 </html>`;
 
-        const fromAddress = smtpUser.includes('gmail.com') ? 'contacto@elenalacosturera.cl' : smtpUser;
-        const info = await transporter.sendMail({
-            from: `"ELENA La Costurera" <${fromAddress}>`,
-            to: customerEmail,
-            subject: 'Presupuesto Formal - ELENA La Costurera',
-            html: htmlContent,
-            attachments: attachments,
-        });
+        if (resendKey) {
+            const res = await fetch('https://api.resend.com/emails', {
+                method: 'POST',
+                headers: {
+                    'Content-Type': 'application/json',
+                    Authorization: `Bearer ${resendKey}`,
+                },
+                body: JSON.stringify({
+                    from: `ELENA La Costurera <${fallbackEmail}>`,
+                    to: [customerEmail],
+                    subject: 'Presupuesto Formal - ELENA La Costurera',
+                    html: htmlContent,
+                }),
+            });
+            if (!res.ok) {
+                const errText = await res.text();
+                throw new Error(`Resend Error: ${errText}`);
+            }
+        } else {
+            console.log('[EDGE DEV] Mock Email enviado a:', customerEmail);
+        }
 
-        console.log('Correo enviado de forma exitosa:', info.messageId);
         return { success: true };
     } catch (err: unknown) {
-        console.error('Error al enviar correo por SMTP de Google:', err);
+        console.error('Error al enviar correo via Resend/Edge:', err);
         return { success: false, error: err instanceof Error ? err.message : String(err) };
     }
 }
@@ -241,15 +222,17 @@ export async function sendOrderConfirmationEmailAction(payload: {
         images?: { url: string; notes?: string }[];
     }[];
     total: number;
+    paidAmount?: number;
     paymentMethod: string;
     date: string;
     deliveryDate: string;
     deliveryWindowStart?: string;
     deliveryWindowEnd?: string;
     paymentUrl?: string;
+    customerPhone?: string | null;
 }) {
     try {
-        const { customerEmail, customerName, orderId, items, total, paymentMethod, date, deliveryDate, deliveryWindowStart, deliveryWindowEnd, paymentUrl: providedPaymentUrl } = payload;
+        const { customerEmail, customerName, customerPhone, orderId, items, total, paidAmount = total, paymentMethod, date, deliveryDate, deliveryWindowStart, deliveryWindowEnd, paymentUrl: providedPaymentUrl } = payload;
 
         // Format delivery date for display
         const deliveryDateObj = new Date(deliveryDate);
@@ -260,23 +243,8 @@ export async function sendOrderConfirmationEmailAction(payload: {
             hour: '2-digit', minute: '2-digit', hour12: false
         });
 
-        const smtpUser = process.env.SMTP_USER || '';
-        const smtpPassword = process.env.SMTP_PASSWORD || '';
-
-        if (!smtpUser || !smtpPassword) {
-            console.error('Faltan variables SMTP_USER o SMTP_PASSWORD en el entorno.');
-            return { success: false, error: 'Credenciales de correo no configuradas en el servidor.' };
-        }
-
-        const transporter = nodemailer.createTransport({
-            host: 'smtp.gmail.com',
-            port: 465,
-            secure: true,
-            auth: {
-                user: smtpUser,
-                pass: smtpPassword,
-            },
-        });
+        const resendKey = process.env.RESEND_API_KEY;
+        const fallbackEmail = 'contacto@elenalacosturera.cl';
 
         const formatCurrency = (val: number) => {
             return new Intl.NumberFormat('es-CL', { style: 'currency', currency: 'CLP', maximumFractionDigits: 0 }).format(val);
@@ -301,21 +269,7 @@ export async function sendOrderConfirmationEmailAction(payload: {
             </tr>
         `).join('');
 
-        const attachments = [];
-        let cardBgUrl = '';
-
-        const filePath = path.join(process.cwd(), 'public', 'trabajos', 'model_desnuda_bw.png');
-        if (fs.existsSync(filePath)) {
-            attachments.push({
-                filename: 'model_desnuda_bw.png',
-                path: filePath,
-                cid: 'luxuryPassBg'
-            });
-            cardBgUrl = 'cid:luxuryPassBg';
-        } else {
-            // Transparent 1x1 fallback so it doesn't break URL loading
-            cardBgUrl = 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAEAAAABACAYAAACqaXHeAAAAGUlEQVR4nO3BMQEAAADCoPVPbQ0PoAAAAAAAAAAA8F8bGgABxZqVdgAAAABJRU5ErkJggg==';
-        }
+        const cardBgUrl = backgroundImgBase64;
 
         const garmentsSectionHtml = `
             <div style="margin-bottom: 24px; text-align: left;">
@@ -333,12 +287,26 @@ export async function sendOrderConfirmationEmailAction(payload: {
                 `).join('')}
               </table>
               <table border="0" cellpadding="0" cellspacing="0" style="width: 100%; border-collapse: collapse; margin-bottom: 16px;">
-                <tr>
-                  <td style="padding: 4px 0; text-align: left; font-size: 8px; font-weight: 600; color: #8A857D; letter-spacing: 2px; text-transform: uppercase; font-family: 'Inter', sans-serif;">Total Pagado</td>
-                  <td style="padding: 4px 0; text-align: right; font-family: 'Playfair Display', Georgia, serif; font-size: 18px; font-weight: 300; color: #C17F5F;">
-                    ${formatCurrency(total)}
-                  </td>
-                </tr>
+                  <table border="0" cellpadding="0" cellspacing="0" style="width: 100%; border-collapse: collapse;">
+                  <tr>
+                    <td style="padding: 4px 0; text-align: left; font-size: 8px; font-weight: 500; color: #8A857D; letter-spacing: 1px; font-family: 'Inter', sans-serif;">Total Orden</td>
+                    <td style="padding: 4px 0; text-align: right; font-family: 'Playfair Display', Georgia, serif; font-size: 11px; color: #E5E0D8;">${formatCurrency(total)}</td>
+                  </tr>
+                  <tr>
+                    <td style="padding: 4px 0; text-align: left; font-size: 8px; font-weight: 600; color: #C17F5F; letter-spacing: 2px; text-transform: uppercase; font-family: 'Inter', sans-serif;">Abono Actual</td>
+                    <td style="padding: 4px 0; text-align: right; font-family: 'Playfair Display', Georgia, serif; font-size: 16px; font-weight: 300; color: #C17F5F;">
+                      ${formatCurrency(paidAmount)}
+                    </td>
+                  </tr>
+                  ${total - paidAmount > 0 ? `
+                  <tr>
+                    <td style="padding: 4px 0; text-align: left; font-size: 8px; font-weight: 600; color: #E5E0D8; letter-spacing: 2px; text-transform: uppercase; font-family: 'Inter', sans-serif; border-top: 1px solid rgba(245, 242, 235, 0.08); margin-top: 4px;">Saldo Pendiente</td>
+                    <td style="padding: 4px 0; text-align: right; font-family: 'Playfair Display', Georgia, serif; font-size: 14px; font-weight: bold; color: #E5E0D8; border-top: 1px solid rgba(245, 242, 235, 0.08); margin-top: 4px;">
+                      ${formatCurrency(total - paidAmount)}
+                    </td>
+                  </tr>
+                  ` : ''}
+                </table>
               </table>
             </div>
             `;
@@ -463,19 +431,92 @@ export async function sendOrderConfirmationEmailAction(payload: {
 </body>
 </html>`;
 
-        const fromAddress = smtpUser.includes('gmail.com') ? 'contacto@elenalacosturera.cl' : smtpUser;
-        const info = await transporter.sendMail({
-            from: `"ELENA La Costurera" <${fromAddress}>`,
-            to: customerEmail,
-            subject: `Tu pieza ya ingres├│ al atelier ÔÇö ELENA La Costurera`,
-            html: htmlContent,
-            attachments: attachments,
-        });
+        if (resendKey && customerEmail) {
+            const res = await fetch('https://api.resend.com/emails', {
+                method: 'POST',
+                headers: {
+                    'Content-Type': 'application/json',
+                    Authorization: `Bearer ${resendKey}`,
+                },
+                body: JSON.stringify({
+                    from: `ELENA La Costurera <${fallbackEmail}>`,
+                    to: [customerEmail],
+                    subject: `Tu pieza ya ingres├│ al atelier ÔÇö ELENA La Costurera`,
+                    html: htmlContent,
+                }),
+            });
+            if (!res.ok) {
+                const errText = await res.text();
+                console.error(`Resend Error: ${errText}`);
+                
+                // Fallback to WhatsApp if email fails
+                const WHATSAPP_PHONE_NUMBER_ID = process.env.WHATSAPP_PHONE_NUMBER_ID;
+                const WHATSAPP_API_TOKEN = process.env.WHATSAPP_API_TOKEN;
+                if (customerPhone && WHATSAPP_PHONE_NUMBER_ID && WHATSAPP_API_TOKEN) {
+                    try {
+                        const cleanPhone = customerPhone.replace(/[^0-9]/g, '');
+                        if (cleanPhone.length >= 9) {
+                            const fullPhone = cleanPhone.startsWith('56') ? cleanPhone : `56${cleanPhone}`;
+                            await fetch(`https://graph.facebook.com/v21.0/${WHATSAPP_PHONE_NUMBER_ID}/messages`, {
+                                method: 'POST',
+                                headers: { 'Authorization': `Bearer ${WHATSAPP_API_TOKEN}`, 'Content-Type': 'application/json' },
+                                body: JSON.stringify({
+                                    messaging_product: 'whatsapp',
+                                    to: fullPhone,
+                                    type: 'template',
+                                    template: {
+                                        name: 'confirmacion_pago_cliente',
+                                        language: { code: 'es_CL' },
+                                        components: [{
+                                            type: 'body',
+                                            parameters: [{ type: 'text', text: firstName }]
+                                        }]
+                                    }
+                                })
+                            });
+                        }
+                    } catch (e) {
+                        console.error('Error enviando WhatsApp de fallback:', e);
+                    }
+                }
+            }
+        } else {
+            console.log('[EDGE DEV] Mock Confirmaci├│n de Orden / No Email. Email:', customerEmail);
+            // Si el cliente no tiene correo, simplemente salta al WhatsApp (Fallback)
+            const WHATSAPP_PHONE_NUMBER_ID = process.env.WHATSAPP_PHONE_NUMBER_ID;
+            const WHATSAPP_API_TOKEN = process.env.WHATSAPP_API_TOKEN;
+            if (customerPhone && WHATSAPP_PHONE_NUMBER_ID && WHATSAPP_API_TOKEN) {
+                try {
+                    const cleanPhone = customerPhone.replace(/[^0-9]/g, '');
+                    if (cleanPhone.length >= 9) {
+                        const fullPhone = cleanPhone.startsWith('56') ? cleanPhone : `56${cleanPhone}`;
+                        await fetch(`https://graph.facebook.com/v21.0/${WHATSAPP_PHONE_NUMBER_ID}/messages`, {
+                            method: 'POST',
+                            headers: { 'Authorization': `Bearer ${WHATSAPP_API_TOKEN}`, 'Content-Type': 'application/json' },
+                            body: JSON.stringify({
+                                messaging_product: 'whatsapp',
+                                to: fullPhone,
+                                type: 'template',
+                                template: {
+                                    name: 'confirmacion_pago_cliente',
+                                    language: { code: 'es_CL' },
+                                    components: [{
+                                        type: 'body',
+                                        parameters: [{ type: 'text', text: firstName }]
+                                    }]
+                                }
+                            })
+                        });
+                    }
+                } catch (e) {
+                    console.error('Error enviando WhatsApp de fallback:', e);
+                }
+            }
+        }
 
-        console.log('Correo de confirmaci├│n enviado:', info.messageId);
         return { success: true };
     } catch (err: unknown) {
-        console.error('Error al enviar correo por SMTP de Google:', err);
+        console.error('Error al enviar correo via Resend/Edge:', err);
         return { success: false, error: err instanceof Error ? err.message : String(err) };
     }
 }
@@ -1313,7 +1354,7 @@ export async function wakeUpMercadoPagoTerminalAction(amount: number, descriptio
             }
         };
 
-        const response = await fetch(`https://api.mercadopago.com/v1/orders`, {
+        let response = await fetch(`https://api.mercadopago.com/v1/orders`, {
             method: 'POST',
             headers: {
                 'Authorization': `Bearer ${mpToken}`,
@@ -1324,9 +1365,40 @@ export async function wakeUpMercadoPagoTerminalAction(amount: number, descriptio
         });
 
         if (!response.ok) {
-            const errData = await response.text();
-            console.error('Error despertando terminal MP:', errData);
-            return { success: false, error: `Error MP Orders API: ${response.status} - ${errData}` };
+            let errData = await response.text();
+            
+            // Si el terminal ya tiene una orden bloqueada, intentamos limpiarla remotamente
+            if (response.status === 409 && errData.includes('already_queued_order_on_terminal')) {
+                console.log('El terminal tiene una orden atascada. Intentando limpiar...');
+                // Limpiar el dispositivo
+                await fetch(`https://api.mercadopago.com/point/integration-api/devices/${terminalId}/payment-intents`, {
+                    method: 'DELETE',
+                    headers: { 'Authorization': `Bearer ${mpToken}` }
+                });
+                
+                // Esperar 1 segundo para que el dispositivo se libere
+                await new Promise(r => setTimeout(r, 1000));
+                
+                // Reintentar crear la orden
+                response = await fetch(`https://api.mercadopago.com/v1/orders`, {
+                    method: 'POST',
+                    headers: {
+                        'Authorization': `Bearer ${mpToken}`,
+                        'Content-Type': 'application/json',
+                        'X-Idempotency-Key': randomUUID()
+                    },
+                    body: JSON.stringify(payload)
+                });
+                
+                if (!response.ok) {
+                    errData = await response.text();
+                    console.error('Error despertando terminal MP tras reintento:', errData);
+                    return { success: false, error: `Error MP Orders API tras reintento: ${response.status} - ${errData}. Por favor presione la X roja en la maquinita f├¡sica para cancelarlo manualmente.` };
+                }
+            } else {
+                console.error('Error despertando terminal MP:', errData);
+                return { success: false, error: `Error MP Orders API: ${response.status} - ${errData}` };
+            }
         }
 
         const data = await response.json();
@@ -1749,16 +1821,16 @@ export async function getMonthAvailabilityAction(year: number, month: number) {
 export async function getPOSOrderAmountAction(posOrderId: string) {
     const supabase = await createClient();
     const { data, error } = await supabase
-        .from('production_orders')
-        .select('total_price')
-        .eq('pos_order_id', posOrderId)
+        .from('sales_ledger')
+        .select('total_amount')
+        .eq('internal_id', posOrderId)
         .limit(1);
 
     if (error || !data || data.length === 0) {
         console.error('Error fetching order amount:', error);
         return { success: false, amount: 0 };
     }
-    return { success: true, amount: data[0].total_price || 0 };
+    return { success: true, amount: data[0].total_amount || 0 };
 }
 
 export async function analyzeDesignWithGeminiAction(base64Image: string) {
@@ -1880,6 +1952,7 @@ export async function cancelPendingOrderAction(orderId: string, isBalancePayment
             if (pendingBalanceRows && pendingBalanceRows.length > 0) {
                 for (const row of pendingBalanceRows) {
                     await supabase.from('sales_ledger').delete().eq('id', row.id);
+                    await supabase.from('cash_movements').delete().like('reason', `%${row.internal_id}%`);
                 }
             }
             
@@ -1888,6 +1961,7 @@ export async function cancelPendingOrderAction(orderId: string, isBalancePayment
         } else {
             await supabase.from('production_orders').delete().eq('pos_order_id', orderId);
             await supabase.from('sales_ledger').delete().eq('internal_id', orderId);
+            await supabase.from('cash_movements').delete().like('reason', `%${orderId}%`);
         }
         
         revalidatePath('/admin/pos');
diff --git a/src/app/admin/pos/page.tsx b/src/app/admin/pos/page.tsx
index b612894..0d78ea8 100644
--- a/src/app/admin/pos/page.tsx
+++ b/src/app/admin/pos/page.tsx
@@ -3,6 +3,8 @@
 import React, { useState, useRef, useEffect } from 'react';
 import Link from 'next/link';
 import { ArrowLeft, ArrowRight, ShoppingCart, User, Search, CreditCard, Tag, X, Plus, MessageSquare, Mail, ClipboardList, TrendingUp, Loader2, Package, Camera, FileText, AlertCircle, Globe, Sparkles, Trash2 } from 'lucide-react';
+import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
+import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
 import { getCostSettings } from '../finance/actions';
 import { getCatalog } from '../catalog/actions';
 import { getCustomers, createCustomer } from '../crm/actions';
@@ -210,9 +212,9 @@ const DEFAULT_HC_TEMPLATES = [
     materiales: 40000,
     extra: 5000
   }
-];
-
-export default function POSPage() {
+];export default function POSPage() {
+    const [mobileTab, setMobileTab] = useState<'form' | 'ticket'>('form');
+    const [isMounted, setIsMounted] = useState(false);
     const [cart, setCart] = useState<any[]>([]);
     const [products, setProducts] = useState<any[]>([]);
     const [loadBalancerModal, setLoadBalancerModal] = useState<{ show: boolean, targetOpId: string, itemIndex: number | null, estimatedHours: number, targetName: string, targetLoadDays: string, alternatives: any[] } | null>(null);
@@ -1081,9 +1083,17 @@ export default function POSPage() {
                     if (!mpRes.success) {
                         console.error('Error despertando terminal Mercado Pago:', mpRes.error);
                         alert(`Error al enviar el cobro a la maquinita f├¡sica: ${mpRes.error}`);
+                        // ROLLBACK! Cancel the order created in the DB because the terminal failed
+                        await cancelPendingOrderAction(finalOrderIdStr, posMode === 'pay_balance');
+                        setIsProcessing(false);
+                        return; // Abort checkout process
                     }
                 } catch (mpErr) {
                     console.error('Excepci├│n al despertar terminal MP:', mpErr);
+                    alert(`Ocurri├│ un error excepcional al contactar la maquinita: ${mpErr}`);
+                    await cancelPendingOrderAction(finalOrderIdStr, posMode === 'pay_balance');
+                    setIsProcessing(false);
+                    return; // Abort checkout process
                 }
             }
             
@@ -1092,23 +1102,35 @@ export default function POSPage() {
                 customer: selectedCustomer,
                 items: [...cart],
                 total: amountToCharge, // In pay_balance, total amount being paid is amountToCharge
+                paidAmount: amountToCharge,
                 method: finalPaymentMethodStr,
                 date: dateStr,
                 deliveryDate: finalDeliveryDateStr,
-                paymentUrl: paymentUrl
+                deliveryWindowStart: adjustedDates?.config?.windowStart?.slice(0, 5) || '15:00',
+                deliveryWindowEnd: adjustedDates?.config?.windowEnd?.slice(0, 5) || '18:00',
+                paymentUrl: paymentUrl,
+                posMode: posMode
             });
             
             // Reset payment confirmed state for physical terminal polling
             setPaymentConfirmed(false);
+            let sendEmailImmediately = false;
             if (paymentMethod === 'cash' && splitCardAmount === 0) {
                 setPaymentConfirmed(true);
+                sendEmailImmediately = true;
+            } else if (paymentMethod === 'transfer') {
+                sendEmailImmediately = true;
+            } else if (paymentMethod === 'transbank') {
+                // Para pagos online, DEBEMOS enviar el correo inmediatamente porque contiene el link de pago
+                sendEmailImmediately = true;
             }
 
-            // Enviar autom├íticamente el correo de confirmaci├│n si no es un pago de saldo
-            if (selectedCustomer.email && posMode === 'new_sale') {
+            // Enviar autom├íticamente el correo (y fallback WhatsApp) si es un pago directo confirmado o si requiere link de pago online
+            if (sendEmailImmediately && (selectedCustomer.email || selectedCustomer.phone) && posMode === 'new_sale') {
                 sendOrderConfirmationEmailAction({
-                    customerEmail: selectedCustomer.email,
+                    customerEmail: selectedCustomer.email || '',
                     customerName: selectedCustomer.full_name,
+                    customerPhone: selectedCustomer.phone,
                     orderId: newOrderIdNumber,
                     items: cart.map(item => ({
                         name: item.name,
@@ -1118,6 +1140,7 @@ export default function POSPage() {
                         images: item.images || []
                     })),
                     total: total,
+                    paidAmount: amountToCharge,
                     paymentMethod: finalPaymentMethodStr || '',
                     date: dateStr,
                     deliveryDate: deadline || adjustedDates?.finalDeliveryDate || '',
@@ -1125,10 +1148,11 @@ export default function POSPage() {
                     deliveryWindowEnd: adjustedDates?.config?.windowEnd?.slice(0, 5) || '18:00',
                     paymentUrl: paymentUrl
                 }).catch(err => {
-                    console.error('Error al enviar correo autom├ítico de confirmaci├│n:', err);
+                    console.error('Error al enviar notificaci├│n autom├ítica de confirmaci├│n:', err);
                 });
             }
             
+
             setCart([]);
             setPaymentMethod(null);
             setDeadline('');
@@ -1320,20 +1344,30 @@ export default function POSPage() {
 
     return (
         <>
-        <div className={`min-h-screen bg-gray-50 flex flex-col lg:flex-row font-sans ${isBudgetModalOpen ? 'print:hidden' : ''}`}>
+        <div className={`grid grid-cols-1 lg:grid-cols-3 gap-8 p-4 lg:p-8 bg-zinc-50 min-h-screen font-sans ${isBudgetModalOpen ? 'print:hidden' : ''}`}>
+            {/* Mobile Tabs Switcher */}
+            <div className="block lg:hidden w-full mb-2">
+                <Tabs value={mobileTab} onValueChange={(v) => setMobileTab(v as any)}>
+                    <TabsList className="w-full grid grid-cols-2">
+                        <TabsTrigger value="form">Nueva Orden</TabsTrigger>
+                        <TabsTrigger value="ticket">Ver Pedido ($)</TabsTrigger>
+                    </TabsList>
+                </Tabs>
+            </div>
+
             {/* Product Selection Area */}
-            <div className="flex-1 p-4 md:p-8 pt-20 space-y-8 overflow-y-auto">
+            <div className={`lg:col-span-2 space-y-8 ${mobileTab === 'form' ? 'block' : 'hidden lg:block'}`}>
                 <div className="mb-10 flex flex-col sm:flex-row justify-between items-start sm:items-end gap-4">
                     <div>
-                        <h2 className="text-3xl sm:text-4xl font-serif text-brand-charcoal mb-2">Crear Nueva Orden</h2>
-                        <p className="text-gray-500 font-sans text-[10px] uppercase tracking-widest flex items-center gap-2">
+                        <h2 className="text-2xl font-medium text-zinc-900 tracking-tight tracking-tight mb-2">Crear Nueva Orden</h2>
+                        <p className="text-gray-500 font-sans text-xs font-normal text-zinc-400 tracking-normal flex items-center gap-2">
                             Punto de Venta ┬À Nueva Venta o Presupuesto
                         </p>
                     </div>
                     {isCajaOpen !== null && (
                         <Link 
                             href="/admin/caja" 
-                            className={`px-5 py-2.5 rounded-full shadow-md flex items-center gap-2 text-[10px] font-bold uppercase tracking-widest transition-all active:scale-95 ${
+                            className={`px-5 py-2.5 rounded-full shadow-md flex items-center gap-2 text-xs font-medium text-zinc-600 tracking-normal transition-all active:scale-95 ${
                                 isCajaOpen 
                                     ? 'bg-green-50 text-green-700 border border-green-200 hover:bg-green-100' 
                                     : 'bg-red-50 text-red-700 border border-red-200 hover:bg-red-100'
@@ -1345,7 +1379,7 @@ export default function POSPage() {
                     )}
                 </div>
                 <div className="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-8">
-                    <h1 className="font-serif text-3xl text-brand-charcoal flex items-center gap-3">
+                    <h1 className="text-2xl font-medium text-zinc-900 tracking-tight tracking-tight flex items-center gap-3">
                         <ClipboardList className="w-8 h-8 text-brand-terracotta" />
                         Ingreso de Orden de Trabajo
                     </h1>
@@ -1368,12 +1402,21 @@ export default function POSPage() {
                                     setIsHauteCoutureModalOpen(true);
                                 }
                             } else {
-                                setHcPrendaName(customOrderName || '');
-                                setIsHauteCoutureModalOpen(true);
+                                // Automatically extract customer name and use it for the Haute Couture Modal
+                                if (selectedCustomer) {
+                                    setHcPrendaName(`${selectedCustomer.full_name} - Alta Costura`);
+                                    setIsHauteCoutureModalOpen(true);
+                                } else if (customOrderName) {
+                                    setHcPrendaName(`${customOrderName} - Alta Costura`);
+                                    setIsHauteCoutureModalOpen(true);
+                                } else {
+                                    setHcPrendaName('');
+                                    setIsHauteCoutureModalOpen(true);
+                                }
                             }
                         }}
                         disabled={isAnalyzing}
-                        className="px-5 py-2.5 bg-brand-charcoal text-white hover:bg-brand-terracotta text-[10px] uppercase tracking-widest font-bold rounded-sm shadow-md transition-all flex items-center gap-2 cursor-pointer active:scale-95 disabled:opacity-50"
+                        className="w-full sm:w-auto justify-center sm:justify-start px-5 py-2.5 bg-brand-charcoal text-white hover:bg-brand-terracotta text-xs font-normal text-zinc-400 tracking-normal font-bold rounded-sm shadow-md transition-all flex items-center gap-2 cursor-pointer active:scale-95 disabled:opacity-50"
                     >
                         {isAnalyzing ? (
                             <><Loader2 className="w-4 h-4 animate-spin" /> Analizando...</>
@@ -1386,8 +1429,8 @@ export default function POSPage() {
                 {/* Unified Search Bar implemented below inside the main customer area */}
 
                 {/* Section 1: Client (Always show for searching either client or pending order) */}
-                <div className="bg-white p-6 md:p-8 rounded-sm border border-gray-100 shadow-sm space-y-4">
-                    <h3 className="text-xs font-bold uppercase tracking-widest text-brand-terracotta border-b border-gray-100 pb-2 flex items-center gap-2 mb-6">
+                <div className="bg-white border border-zinc-100 shadow-[0_8px_30px_rgb(0,0,0,0.04)] rounded-2xl p-8 space-y-4">
+                    <h3 className="text-sm font-medium text-zinc-500 uppercase tracking-wider border-b border-gray-100 pb-2 flex items-center gap-2 mb-6">
                         <User className="w-4 h-4" /> 1. Identificaci├│n de Cliente
                     </h3>
                     
@@ -1405,17 +1448,17 @@ export default function POSPage() {
                             }} className="text-xs uppercase tracking-widest text-gray-400 hover:text-red-500 font-bold">Cambiar Cliente / Limpiar</button>
                         </div>
                     ) : (
-                        <div className="space-y-6">
+                        <div className="space-y-8">
                             {/* Search and New Button Bar */}
-                            <div className="flex flex-col md:flex-row gap-4">
-                                <div className="relative flex-1">
+                            <div className="flex flex-col sm:flex-row gap-4 sm:items-center w-full">
+                                <div className="relative w-full sm:flex-1">
                                     <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400" />
                                     <input 
                                         type="text" 
                                         placeholder="Buscar por nombre, rut o correo..." 
                                         value={clientSearch}
                                         onChange={(e) => setClientSearch(e.target.value)}
-                                        className="w-full pl-10 pr-4 py-3 bg-white border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta text-sm"
+                                        className="w-full pl-10 pr-4 py-3 bg-white border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta text-sm"
                                     />
                                     
                                     {/* Search Results Dropdown */}
@@ -1454,7 +1497,7 @@ export default function POSPage() {
                                                             >
                                                                 <div>
                                                                     <p className="font-bold text-sm text-brand-charcoal">{order.customers?.full_name}</p>
-                                                                    <p className="text-[10px] font-mono text-gray-500 uppercase tracking-tighter">
+                                                                    <p className="text-xs font-mono text-gray-500 uppercase tracking-tighter">
                                                                         PAGAR SALDO ORDEN #{order.pos_order_id}
                                                                     </p>
                                                                 </div>
@@ -1482,15 +1525,15 @@ export default function POSPage() {
                                                 }} className="p-4 hover:bg-brand-sand/10 cursor-pointer border-b border-gray-50 last:border-0 flex justify-between items-center group">
                                                     <div>
                                                         <p className="text-sm font-medium text-brand-charcoal">{c.full_name}</p>
-                                                        <p className="text-[10px] text-gray-400 font-mono tracking-tighter uppercase">{c.email} | {c.phone}</p>
+                                                        <p className="text-xs text-gray-400 font-mono tracking-tighter uppercase">{c.email} | {c.phone}</p>
                                                     </div>
                                                     <ArrowRight className="w-4 h-4 text-gray-300 group-hover:text-brand-terracotta transition-all" />
                                                 </div>
                                             ))}
                                             {allCustomers.filter(c => c.full_name.toLowerCase().includes(clientSearch.toLowerCase()) || (c.email && c.email.toLowerCase().includes(clientSearch.toLowerCase()))).length === 0 && (
-                                                <div className="p-6 text-center">
+                                                <div className="p-8 text-center">
                                                     <p className="text-xs text-gray-500 italic mb-2">No se encontr├│ cliente con "{clientSearch}"</p>
-                                                    <button onClick={() => { setIsRegisteringClient(true); setClientSearch(''); }} className="text-[10px] uppercase tracking-widest font-bold text-brand-terracotta hover:underline">+ Crear ficha de cliente nuevo</button>
+                                                    <button onClick={() => { setIsRegisteringClient(true); setClientSearch(''); }} className="text-xs font-normal text-zinc-400 tracking-normal font-bold text-brand-terracotta hover:underline">+ Crear ficha de cliente nuevo</button>
                                                 </div>
                                             )}
                                         </div>
@@ -1501,7 +1544,7 @@ export default function POSPage() {
                                         setIsRegisteringClient(!isRegisteringClient);
                                         setNewClientData({ name: '', phone: '', email: '' });
                                     }}
-                                    className={`px-6 py-3 text-[10px] uppercase tracking-widest font-bold rounded-sm transition-all flex items-center gap-2 justify-center border ${isRegisteringClient ? 'bg-white border-red-200 text-red-500' : 'bg-brand-charcoal text-white border-brand-charcoal hover:bg-brand-terracotta'}`}
+                                    className={`w-full sm:w-auto px-6 py-3 text-xs font-normal text-zinc-400 tracking-normal font-bold rounded-sm transition-all flex items-center gap-2 justify-center border ${isRegisteringClient ? 'bg-white border-red-200 text-red-500' : 'bg-brand-charcoal text-white border-brand-charcoal hover:bg-brand-terracotta'}`}
                                 >
                                     {isRegisteringClient ? <X className="w-4 h-4" /> : <Plus className="w-4 h-4" />} 
                                     {isRegisteringClient ? 'Cancelar' : 'Nuevo Cliente'}
@@ -1512,7 +1555,7 @@ export default function POSPage() {
 
                             {/* Quick Registration Form (Expandable) */}
                             {isRegisteringClient && (
-                                <div className="bg-white p-8 rounded-sm border border-gray-100 space-y-8 animate-in fade-in slide-in-from-top-4 duration-500 shadow-sm relative">
+                                <div className="bg-white p-8 rounded-sm border border-gray-100 space-y-8 animate-in fade-in slide-in-from-top-4 duration-500 shadow-[0_8px_30px_rgb(0,0,0,0.04)] relative">
                                     <button 
                                         onClick={() => setIsRegisteringClient(false)}
                                         className="absolute top-4 right-4 text-gray-300 hover:text-red-500 transition-colors"
@@ -1521,8 +1564,8 @@ export default function POSPage() {
                                     </button>
 
                                     <div className="text-center space-y-1">
-                                        <h4 className="text-[10px] uppercase tracking-[0.3em] font-bold text-brand-terracotta">Nueva Ficha de Cliente</h4>
-                                        <p className="text-[10px] text-gray-400 uppercase tracking-widest">Ingrese los datos para registro oficial en CRM</p>
+                                        <h4 className="text-xs uppercase tracking-[0.3em] font-bold text-brand-terracotta">Nueva Ficha de Cliente</h4>
+                                        <p className="text-xs text-gray-400 uppercase tracking-widest">Ingrese los datos para registro oficial en CRM</p>
                                     </div>
 
                                     <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
@@ -1541,7 +1584,7 @@ export default function POSPage() {
                                                         ? newClientData.name.trim().split(/\s+/).length < 2 
                                                             ? 'border-red-300 bg-red-50 focus:border-red-500' 
                                                             : 'border-green-500/30 bg-green-50/30 focus:border-green-500' 
-                                                        : 'bg-gray-50 border-gray-200 focus:border-brand-terracotta focus:bg-white'
+                                                        : 'bg-gray-50 border-zinc-100 focus:border-brand-terracotta focus:bg-white'
                                                 }`} 
                                             />
                                         </div>
@@ -1552,9 +1595,9 @@ export default function POSPage() {
                                                     ? (newClientData.phone.length < 8 || newClientData.phone.length > 12) 
                                                         ? 'border-red-300 bg-red-50 focus-within:border-red-500' 
                                                         : 'border-green-500/30 bg-green-50/30 focus-within:border-green-500' 
-                                                    : 'bg-gray-50 border-gray-200 focus-within:border-brand-terracotta'
+                                                    : 'bg-gray-50 border-zinc-100 focus-within:border-brand-terracotta'
                                             }`}>
-                                                <select className="bg-transparent border-r border-gray-200 p-2 text-[10px] uppercase font-bold outline-none cursor-pointer text-gray-600">
+                                                <select className="w-auto border-none bg-transparent shadow-none focus:ring-0 px-2 text-xs font-medium min-h-[44px] border-r border-zinc-100 rounded-none cursor-pointer outline-none" value="56" onChange={() => {}}>
                                                     <option value="56">­ƒç¿­ƒç▒ +56</option>
                                                     <option value="54">­ƒçª­ƒçÀ +54</option>
                                                     <option value="55">­ƒçº­ƒçÀ +55</option>
@@ -1585,7 +1628,7 @@ export default function POSPage() {
                                                         ? !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(newClientData.email) 
                                                             ? 'border-red-300 bg-red-50 focus:border-red-500' 
                                                             : 'border-green-500/30 bg-green-50/30 focus:border-green-500' 
-                                                        : 'bg-gray-50 border-gray-200 focus:border-brand-terracotta focus:bg-white'
+                                                        : 'bg-gray-50 border-zinc-100 focus:border-brand-terracotta focus:bg-white'
                                                 }`} 
                                             />
                                         </div>
@@ -1600,7 +1643,7 @@ export default function POSPage() {
                                                 newClientData.phone.length < 8 ||
                                                 !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(newClientData.email)
                                             } 
-                                            className="w-full md:w-[400px] py-4 bg-brand-charcoal text-white text-[10px] uppercase tracking-[0.2em] font-bold hover:bg-brand-terracotta transition-all disabled:opacity-20 disabled:bg-gray-400 rounded-sm shadow-md active:scale-95 text-center"
+                                            className="w-full md:w-[400px] py-4 bg-brand-charcoal text-white text-xs uppercase tracking-[0.2em] font-bold hover:bg-brand-terracotta transition-all disabled:opacity-20 disabled:bg-gray-400 rounded-sm shadow-md active:scale-95 text-center"
                                         >
                                             Registrar
                                         </button>
@@ -1614,23 +1657,23 @@ export default function POSPage() {
                 {posMode === 'new_sale' && (
                     <>
                         {/* Section 2: Asignar Costurera */}
-                        <div className={`bg-white p-6 md:p-8 rounded-sm border border-gray-100 shadow-sm space-y-6 transition-all ${!selectedCustomer ? 'opacity-50 pointer-events-none' : ''}`}>
-                            <h3 className="text-xs font-bold uppercase tracking-widest text-brand-terracotta border-b border-gray-100 pb-2 flex items-center gap-2 mb-6">
+                        <div className={`bg-white border border-zinc-100 shadow-[0_8px_30px_rgb(0,0,0,0.04)] rounded-2xl p-8 space-y-8 transition-all ${!selectedCustomer ? 'opacity-50 pointer-events-none' : ''}`}>
+                            <h3 className="text-sm font-medium text-zinc-500 uppercase tracking-wider border-b border-gray-100 pb-2 flex items-center gap-2 mb-6">
                                 <Tag className="w-4 h-4" /> 2. Asignar Costurera
                             </h3>
                             
                             <div className="grid grid-cols-2 gap-4">
                                 <div className="col-span-2">
-                                    <label className="block text-[10px] uppercase tracking-widest text-brand-charcoal font-bold mb-1 flex items-center gap-1.5">
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-brand-charcoal font-bold mb-1 flex items-center gap-1.5">
                                         ­ƒæñ Costurera / Operaria Asignada
                                     </label>
-                                    <p className="text-[10px] text-gray-500 leading-normal mb-2">Selecciona la costurera para calcular la agenda y tiempos de entrega autom├íticamente.</p>
+                                    <p className="text-xs text-gray-500 leading-normal mb-2">Selecciona la costurera para calcular la agenda y tiempos de entrega autom├íticamente.</p>
                                     <select
-                                        value={assignedOperatorId}
-                                        onChange={(e) => handleOperatorSelection(e.target.value, null, selectedCatalogProduct ? Number(selectedCatalogProduct.estimated_hours || 2) : (Number(hoursEstimated) || 2))}
-                                        className="w-full p-3 text-sm font-medium bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta transition-colors"
+                                        value={assignedOperatorId || "unassigned"}
+                                        onChange={(e) => handleOperatorSelection(e.target.value === "unassigned" ? "" : e.target.value, null, selectedCatalogProduct ? Number(selectedCatalogProduct.estimated_hours || 2) : (Number(hoursEstimated) || 2))}
+                                        className="w-full h-11 px-4 text-sm font-medium bg-white border border-zinc-200/50 rounded-2xl outline-none focus:ring-2 focus:ring-zinc-100 transition-all shadow-[0_2px_10px_rgb(0,0,0,0.02)] cursor-pointer"
                                     >
-                                        <option value="" disabled>-- Selecciona Costurera o Taller General --</option>
+                                        <option value="unassigned" disabled>-- Selecciona Costurera o Taller --</option>
                                         {operators.map((op: any) => (
                                             <option key={op.id} value={op.id}>
                                                 {op.name} ({op.daily_hours_capacity}h por d├¡a)
@@ -1642,15 +1685,16 @@ export default function POSPage() {
                         </div>
 
                         {/* Section 3: Detalle del Trabajo */}
-                        <div className="bg-white p-6 md:p-8 rounded-sm border border-gray-100 shadow-sm space-y-6 transition-all">
-                            <h3 className="text-xs font-bold uppercase tracking-widest text-brand-terracotta border-b border-gray-100 pb-2 flex items-center gap-2 mb-6">
+                        <div className="bg-white border border-zinc-100 shadow-[0_8px_30px_rgb(0,0,0,0.04)] rounded-2xl p-8 space-y-8 transition-all">
+                            <h3 className="text-sm font-medium text-zinc-500 uppercase tracking-wider border-b border-gray-100 pb-2 flex items-center gap-2 mb-6">
                                 <Tag className="w-4 h-4" /> 3. Detalle del Trabajo
                             </h3>
                             
                             <div className={`grid grid-cols-1 ${customOrderCategory !== 'Cat├ílogo de servicios' ? 'md:grid-cols-3' : 'md:grid-cols-2'} gap-4`}>
                                 <div className="col-span-1">
-                                    <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1">Categor├¡a Principal</label>
-                                    <select value={customOrderCategory} onChange={(e) => setCustomOrderCategory(e.target.value)} className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta">
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1">Categor├¡a Principal</label>
+                                    <select value={customOrderCategory} onChange={(e) => setCustomOrderCategory(e.target.value)} className="w-full h-11 px-4 text-sm font-medium bg-white border border-zinc-200/50 rounded-2xl outline-none focus:ring-2 focus:ring-zinc-100 transition-all shadow-[0_2px_10px_rgb(0,0,0,0.02)] cursor-pointer">
+                                        <option value="" disabled>Seleccionar...</option>
                                         <option value="Dise├▒o y confecci├│n">Dise├▒o y confecci├│n</option>
                                         <option value="Arreglos especializados">Arreglos especializados</option>
                                         <option value="Cat├ílogo de servicios">Cat├ílogo de servicios</option>
@@ -1659,12 +1703,12 @@ export default function POSPage() {
                                 {customOrderCategory !== 'Cat├ílogo de servicios' && (
                                     <>
                                         <div className="col-span-1">
-                                            <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1">Prenda / Art├¡culo *</label>
-                                            <input spellCheck={true} autoCorrect="on" type="text" value={customOrderName} onChange={(e) => setCustomOrderName(e.target.value.charAt(0).toUpperCase() + e.target.value.slice(1))} placeholder="Ej. Pantal├│n, vestido de novia, chaqueta, etc." className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta" />
+                                            <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1">Prenda / Art├¡culo *</label>
+                                            <input spellCheck={true} autoCorrect="on" type="text" value={customOrderName} onChange={(e) => setCustomOrderName(e.target.value.charAt(0).toUpperCase() + e.target.value.slice(1))} placeholder="Ej. Pantal├│n, vestido de novia, chaqueta, etc." className="w-full p-3 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta" />
                                         </div>
                                         <div className="col-span-1">
-                                            <label className="block text-[10px] uppercase tracking-widest font-bold text-gray-500 mb-1">Horas Taller Estimadas</label>
-                                            <input type="number" min="0" value={hoursEstimated || ''} onChange={(e) => setHoursEstimated(Number(e.target.value))} className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:ring-1 focus:ring-brand-terracotta" placeholder="0" />
+                                            <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-500 mb-1">Horas Taller Estimadas</label>
+                                            <input type="number" min="0" value={hoursEstimated || ''} onChange={(e) => setHoursEstimated(Number(e.target.value))} className="w-full p-3 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:ring-1 focus:ring-brand-terracotta" placeholder="0" />
                                         </div>
 
 
@@ -1673,19 +1717,20 @@ export default function POSPage() {
                             </div>
 
                             {customOrderCategory === 'Cat├ílogo de servicios' ? (
-                                <div className="space-y-6 pt-4 border-t border-gray-50 animate-in fade-in duration-500">
-                                    <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
+                                <div className="space-y-8 pt-4 border-t border-gray-50 animate-in fade-in duration-500">
+                                    <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
                                         <div className="space-y-1">
-                                            <label className="block text-[10px] uppercase tracking-widest font-bold text-gray-500 mb-1">Tipo de Prenda / Subcategor├¡a</label>
+                                            <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-500 mb-1">Tipo de Prenda / Subcategor├¡a</label>
                                             <select 
-                                                value={selectedCatalogCategory} 
+                                                value={selectedCatalogCategory || "none"} 
                                                 onChange={(e) => {
-                                                    setSelectedCatalogCategory(e.target.value);
+                                                    const val = e.target.value;
+                                                    setSelectedCatalogCategory(val === "none" ? "" : val);
                                                     setSelectedCatalogProduct(null);
                                                 }} 
-                                                className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta cursor-pointer font-medium transition-all"
+                                                className="w-full h-11 px-4 text-sm font-medium bg-white border border-zinc-200/50 rounded-2xl outline-none focus:ring-2 focus:ring-zinc-100 transition-all shadow-[0_2px_10px_rgb(0,0,0,0.02)] cursor-pointer"
                                             >
-                                                <option value="">-- Seleccionar Subcategor├¡a --</option>
+                                                <option value="none">-- Seleccionar Subcategor├¡a --</option>
                                                 {Array.from(new Set(products.map(p => p.category))).sort().map(cat => (
                                                     <option key={cat} value={cat}>{cat}</option>
                                                 ))}
@@ -1693,18 +1738,18 @@ export default function POSPage() {
                                         </div>
 
                                         <div className="space-y-1">
-                                            <label className="block text-[10px] uppercase tracking-widest font-bold text-gray-500 mb-1">Servicio / Operaci├│n</label>
+                                            <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-500 mb-1">Servicio / Operaci├│n</label>
                                             <select 
                                                 disabled={!selectedCatalogCategory}
-                                                value={selectedCatalogProduct ? selectedCatalogProduct.id : ''} 
+                                                value={selectedCatalogProduct ? selectedCatalogProduct.id.toString() : "none"} 
                                                 onChange={(e) => {
-                                                    const prodId = e.target.value;
-                                                    const found = products.find(p => p.id.toString() === prodId.toString() || p.id === Number(prodId));
+                                                    const val = e.target.value;
+                                                    const found = products.find(p => p.id.toString() === val || p.id === Number(val));
                                                     setSelectedCatalogProduct(found || null);
                                                 }} 
-                                                className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta disabled:opacity-50 disabled:cursor-not-allowed cursor-pointer font-medium transition-all"
+                                                className="w-full h-11 px-4 text-sm font-medium bg-white border border-zinc-200/50 rounded-2xl outline-none focus:ring-2 focus:ring-zinc-100 transition-all shadow-[0_2px_10px_rgb(0,0,0,0.02)] disabled:opacity-50 disabled:cursor-not-allowed cursor-pointer"
                                             >
-                                                <option value="">
+                                                <option value="none">
                                                     {!selectedCatalogCategory 
                                                         ? 'Primero seleccione una subcategor├¡a...' 
                                                         : '-- Seleccionar Servicio --'}
@@ -1713,7 +1758,7 @@ export default function POSPage() {
                                                     .filter(p => p.category === selectedCatalogCategory)
                                                     .sort((a, b) => a.name.localeCompare(b.name))
                                                     .map(p => (
-                                                        <option key={p.id} value={p.id}>
+                                                        <option key={p.id} value={p.id.toString()}>
                                                             {p.name} ({formatCurrency(p.price)})
                                                         </option>
                                                     ))
@@ -1726,21 +1771,21 @@ export default function POSPage() {
 
                                     {/* Beautiful visual feedback box when a service is selected */}
                                     {selectedCatalogProduct && (
-                                        <div className="bg-brand-sand/5 p-6 rounded-sm border border-brand-sand/30 animate-in fade-in slide-in-from-bottom-2 duration-300 space-y-6 mt-6">
+                                        <div className="bg-brand-sand/5 p-8 rounded-sm border border-brand-sand/30 animate-in fade-in slide-in-from-bottom-2 duration-300 space-y-8 mt-6">
                                             {/* Item-specific Notes and Image inside Catalog view */}
-                                            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
+                                            <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
                                                 <div className="space-y-1">
-                                                    <label className="block text-[10px] uppercase tracking-widest font-bold text-gray-500 mb-1">Descripci├│n del Arreglo / Notas Especiales</label>
+                                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-500 mb-1">Descripci├│n del Arreglo / Notas Especiales</label>
                                                     <textarea 
                                                         spellCheck={true} autoCorrect="on"
                                                         value={orderNotes}
                                                         onChange={(e) => setOrderNotes(e.target.value.charAt(0).toUpperCase() + e.target.value.slice(1))}
                                                         placeholder="Ej. Ajuste de hombros vestido seda, arreglar dobladillo, etc."
-                                                        className="w-full h-[120px] p-3 text-xs bg-white border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta resize-none transition-all"
+                                                        className="w-full h-[120px] p-3 text-xs bg-white border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta resize-none transition-all"
                                                     />
                                                 </div>
                                                 <div className="space-y-1">
-                                                    <label className="block text-[10px] uppercase tracking-widest font-bold text-gray-500 mb-1">Fotos de Referencia (Hasta 4)</label>
+                                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-500 mb-1">Fotos de Referencia (Hasta 4)</label>
                                                     {orderImages.length > 0 ? (
                                                         <div className="space-y-2 h-[120px] flex flex-col justify-between">
                                                             <div className="flex flex-wrap items-center gap-2">
@@ -1751,7 +1796,7 @@ export default function POSPage() {
                                                                         className={`relative w-[50px] h-[50px] border rounded-sm overflow-hidden flex items-center justify-center cursor-pointer bg-white transition-all ${
                                                                             idx === activeImageIndex 
                                                                                 ? 'border-brand-terracotta ring-1 ring-brand-terracotta' 
-                                                                                : 'border-gray-200 hover:border-gray-300'
+                                                                                : 'border-zinc-100 hover:border-gray-300'
                                                                         }`}
                                                                     >
                                                                         <img src={img.url} alt={`Prenda ${idx + 1}`} className="w-full h-full object-contain" />
@@ -1802,12 +1847,12 @@ export default function POSPage() {
                                                                         setOrderImages(prev => prev.map((img, i) => i === activeImageIndex ? { ...img, notes: val } : img));
                                                                     }}
                                                                     placeholder={`Indicaciones para foto ${activeImageIndex + 1}...`}
-                                                                    className="w-full p-2 text-xs bg-white border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta transition-all"
+                                                                    className="w-full p-2 text-xs bg-white border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta transition-all"
                                                                 />
                                                             )}
                                                         </div>
                                                     ) : (
-                                                        <div className="border border-dashed border-gray-200 rounded-sm h-[120px] bg-white hover:bg-gray-50 transition-colors flex flex-col items-center justify-center cursor-pointer relative group">
+                                                        <div className="border border-dashed border-zinc-100 rounded-sm h-[120px] bg-white hover:bg-gray-50 transition-colors flex flex-col items-center justify-center cursor-pointer relative group">
                                                             <input 
                                                                 type="file" 
                                                                 accept="image/*"
@@ -1832,10 +1877,10 @@ export default function POSPage() {
 
                                             {/* Section 4: Detalle Precio Orden (Cat├ílogo) */}
                                             <div className={`transition-all mt-6 ${(!selectedCustomer || assignedOperatorId === '' || !selectedCatalogProduct) ? 'opacity-50 pointer-events-none' : ''}`}>
-                                                <h3 className="text-xs font-bold uppercase tracking-widest text-brand-terracotta pb-2 flex items-center gap-2 mb-2 ml-1">
+                                                <h3 className="text-sm font-medium text-zinc-500 uppercase tracking-wider pb-2 flex items-center gap-2 mb-2 ml-1">
                                                     <Tag className="w-4 h-4" /> 4. Detalle de Precio Orden
                                                 </h3>
-                                                <div className="flex flex-col sm:flex-row justify-between items-center gap-4 bg-brand-charcoal text-white p-6 rounded-sm shadow-lg">
+                                                <div className="flex flex-col sm:flex-row justify-between items-center gap-4 bg-brand-charcoal text-white p-8 rounded-sm shadow-lg">
                                                     <div className="text-center sm:text-left">
                                                     <span className="bg-brand-terracotta text-white px-2 py-0.5 rounded-[2px] text-[8px] uppercase tracking-widest font-bold mb-1.5 inline-block">
                                                         {selectedCatalogProduct.category}
@@ -1858,7 +1903,7 @@ export default function POSPage() {
                                                         setActiveImageIndex(0);
                                                         setAssignedOperatorId('');
                                                     }}
-                                                    className="w-full sm:w-auto bg-brand-terracotta text-white px-10 py-4 text-[10px] uppercase tracking-widest font-bold rounded-sm hover:bg-white hover:text-brand-terracotta transition-all shadow-md active:scale-95 text-center"
+                                                    className="w-full sm:w-auto bg-brand-terracotta text-white px-10 py-4 text-xs font-normal text-zinc-400 tracking-normal font-bold rounded-sm hover:bg-white hover:text-brand-terracotta transition-all shadow-md active:scale-95 text-center"
                                                 >
                                                     A├▒adir a la Orden
                                                 </button>
@@ -1868,22 +1913,22 @@ export default function POSPage() {
                                     )}
                                 </div>
                             ) : (
-                                <div className="space-y-6 pt-4 border-t border-gray-50 animate-in fade-in duration-500">
+                                <div className="space-y-8 pt-4 border-t border-gray-50 animate-in fade-in duration-500">
                                     {/* Calculator CTA removed as requested, using top right button instead */}
                                     {/* Notes and Photo attachment for Custom Order */}
-                                    <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
+                                    <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
                                         <div className="space-y-1">
-                                            <label className="block text-[10px] uppercase tracking-widest font-bold text-gray-500 mb-1">Descripci├│n del Arreglo / Notas Especiales</label>
+                                            <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-500 mb-1">Descripci├│n del Arreglo / Notas Especiales</label>
                                             <textarea 
                                                 spellCheck={true} autoCorrect="on"
                                                 value={orderNotes}
                                                 onChange={(e) => setOrderNotes(e.target.value.charAt(0).toUpperCase() + e.target.value.slice(1))}
                                                 placeholder="Ej. Ajuste de hombros vestido seda, arreglar dobladillo, etc."
-                                                className="w-full h-[120px] p-3 text-xs bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta resize-none transition-all focus:bg-white"
+                                                className="w-full h-[120px] p-3 text-xs bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta resize-none transition-all focus:bg-white"
                                             />
                                         </div>
                                         <div className="space-y-1">
-                                            <label className="block text-[10px] uppercase tracking-widest font-bold text-gray-500 mb-1">Foto de Referencia (Prenda - Hasta 4)</label>
+                                            <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-500 mb-1">Foto de Referencia (Prenda - Hasta 4)</label>
                                             {orderImages.length > 0 ? (
                                                 <div className="space-y-2 h-[120px] flex flex-col justify-between">
                                                     <div className="flex flex-wrap items-center gap-2">
@@ -1894,7 +1939,7 @@ export default function POSPage() {
                                                                 className={`relative w-[50px] h-[50px] border rounded-sm overflow-hidden flex items-center justify-center cursor-pointer bg-white transition-all ${
                                                                     idx === activeImageIndex 
                                                                         ? 'border-brand-terracotta ring-1 ring-brand-terracotta' 
-                                                                        : 'border-gray-200 hover:border-gray-300'
+                                                                        : 'border-zinc-100 hover:border-gray-300'
                                                                 }`}
                                                             >
                                                                 <img src={img.url} alt={`Prenda Custom ${idx + 1}`} className="w-full h-full object-contain" />
@@ -1945,12 +1990,12 @@ export default function POSPage() {
                                                                 setOrderImages(prev => prev.map((img, i) => i === activeImageIndex ? { ...img, notes: val } : img));
                                                             }}
                                                             placeholder={`Indicaciones para foto ${activeImageIndex + 1}...`}
-                                                            className="w-full p-2 text-xs bg-white border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta transition-all"
+                                                            className="w-full p-2 text-xs bg-white border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta transition-all"
                                                         />
                                                     )}
                                                 </div>
                                             ) : (
-                                                <div className="border border-dashed border-gray-200 rounded-sm h-[120px] bg-white hover:bg-gray-50 transition-colors flex flex-col items-center justify-center cursor-pointer relative group">
+                                                <div className="border border-dashed border-zinc-100 rounded-sm h-[120px] bg-white hover:bg-gray-50 transition-colors flex flex-col items-center justify-center cursor-pointer relative group">
                                                     <input 
                                                         type="file" 
                                                         accept="image/*"
@@ -1975,16 +2020,16 @@ export default function POSPage() {
 
                         {/* Section 4: Detalle Precio Orden (A Medida) */}
                         <div className={`transition-all mt-6 ${(!selectedCustomer || assignedOperatorId === '' || !customOrderName.trim()) ? 'opacity-50 pointer-events-none' : ''}`}>
-                            <h3 className="text-xs font-bold uppercase tracking-widest text-brand-terracotta pb-2 flex items-center gap-2 mb-2 ml-1">
+                            <h3 className="text-sm font-medium text-zinc-500 uppercase tracking-wider pb-2 flex items-center gap-2 mb-2 ml-1">
                                 <Tag className="w-4 h-4" /> 4. Detalle de Precio Orden
                             </h3>
-                            <div className="flex flex-col md:flex-row justify-between items-center bg-brand-charcoal text-white p-6 rounded-sm shadow-lg gap-6">
+                            <div className="flex flex-col md:flex-row justify-between items-center bg-brand-charcoal text-white p-8 rounded-sm shadow-lg gap-8">
                                         <div className="text-center md:text-left">
-                                            <p className="text-[10px] uppercase tracking-widest text-brand-sand font-bold mb-1">Precio Sugerido (Con Margen {marginPercentage}%)</p>
+                                            <p className="text-xs font-normal text-zinc-400 tracking-normal text-brand-sand font-bold mb-1">Precio Sugerido (Con Margen {marginPercentage}%)</p>
                                             <p className="text-3xl font-serif text-white">{formatCurrency(calculatedPrice)}</p>
                                         </div>
                                         <div className="flex flex-col items-center md:items-start gap-2">
-                                            <label className="text-[10px] uppercase tracking-widest text-brand-sand font-bold">Precio Cobrado Real (CLP)</label>
+                                            <label className="text-xs font-normal text-zinc-400 tracking-normal text-brand-sand font-bold">Precio Cobrado Real (CLP)</label>
                                             <div className="flex items-center gap-2">
                                                 <div className="relative">
                                                     <span className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-sm font-bold">$</span>
@@ -2028,7 +2073,7 @@ export default function POSPage() {
                                         <button 
                                             onClick={(e) => handleAddCustomOrder(e)}
                                             disabled={!customOrderName || !hoursEstimated || !customPrice}
-                                            className="w-full md:w-auto bg-brand-terracotta text-white px-10 py-4 text-[10px] uppercase tracking-widest font-bold rounded-sm hover:bg-white hover:text-brand-terracotta transition-all disabled:opacity-50 disabled:cursor-not-allowed cursor-pointer shadow-md">
+                                            className="w-full md:w-auto bg-brand-terracotta text-white px-10 py-4 text-xs font-normal text-zinc-400 tracking-normal font-bold rounded-sm hover:bg-white hover:text-brand-terracotta transition-all disabled:opacity-50 disabled:cursor-not-allowed cursor-pointer shadow-md">
                                             A├▒adir a la Orden
                                         </button>
                                     </div>
@@ -2041,7 +2086,7 @@ export default function POSPage() {
             </div>
 
             {/* Cart Summary & Checkout */}
-            <div className="w-full lg:w-[450px] bg-white border-t lg:border-t-0 lg:border-l border-gray-200 p-6 md:p-8 pt-8 md:pt-24 shadow-2xl flex flex-col h-[500px] lg:h-auto">
+            <div className={`lg:col-span-1 sticky top-8 bg-white border border-zinc-100 shadow-[0_8px_30px_rgb(0,0,0,0.04)] rounded-2xl p-8 flex flex-col max-h-[calc(100vh-3rem)] overflow-y-auto ${mobileTab === 'ticket' ? 'block' : 'hidden lg:block'}`}>
                 <div className="flex items-center gap-3 mb-8">
                     <ShoppingCart className="w-6 h-6 text-brand-charcoal" />
                     <h2 className="font-serif text-2xl">Pedido Actual</h2>
@@ -2084,7 +2129,7 @@ export default function POSPage() {
                                             <select
                                                 value={item.assignedOperatorId || 'unassigned'}
                                                 onChange={(e) => handleOperatorSelection(e.target.value, i, Number(item.estimatedHours || 2))}
-                                                className="flex-1 text-[10px] uppercase font-semibold text-brand-charcoal bg-white border border-gray-200 outline-none p-2 rounded-sm focus:border-brand-sand cursor-pointer"
+                                                className="flex-1 h-9 px-3 text-xs font-medium bg-white border border-zinc-200/50 rounded-xl outline-none focus:ring-2 focus:ring-zinc-100 shadow-sm transition-all cursor-pointer"
                                             >
                                                 <option value="unassigned">Sin asignar (Taller)</option>
                                                 {operators.map((op: any) => (
@@ -2122,7 +2167,7 @@ export default function POSPage() {
                                                                     newCart[i] = { ...newCart[i], scheduledStartDate: e.target.value };
                                                                     setCart(newCart);
                                                                 }}
-                                                                className="flex-1 text-xs p-1.5 bg-white border border-gray-200 text-brand-charcoal rounded-sm focus:border-brand-sand outline-none"
+                                                                className="flex-1 text-xs p-1.5 bg-white border border-zinc-100 text-brand-charcoal rounded-sm focus:border-brand-sand outline-none"
                                                             />
                                                         </div>
                                                     )}
@@ -2133,7 +2178,7 @@ export default function POSPage() {
                                     
                                     {/* Display individual image notes in cart */}
                                     {item.images && item.images.map((img: any, idx: number) => img.notes && (
-                                        <p key={idx} className="text-[10px] text-brand-terracotta mt-1 leading-normal flex items-start gap-1">
+                                        <p key={idx} className="text-xs text-brand-terracotta mt-1 leading-normal flex items-start gap-1">
                                             <span className="font-bold uppercase text-[7px] bg-brand-terracotta/10 px-1 py-0.5 rounded-[2px] shrink-0 mt-0.5">Indicaci├│n Foto {idx + 1}:</span>
                                             <span className="italic">"{img.notes}"</span>
                                         </p>
@@ -2143,7 +2188,7 @@ export default function POSPage() {
                                 <div className="flex flex-col gap-1 items-end shrink-0">
                                     <div className="flex gap-1 flex-wrap justify-end max-w-[120px]">
                                         {item.images && item.images.map((img: any, idx: number) => (
-                                            <div key={idx} className="w-8 h-8 border border-gray-200 rounded-sm overflow-hidden bg-white flex items-center justify-center shadow-sm">
+                                            <div key={idx} className="w-8 h-8 border border-zinc-100 rounded-sm overflow-hidden bg-white flex items-center justify-center shadow-[0_8px_30px_rgb(0,0,0,0.04)]">
                                                 <img src={img.url} alt={`Prenda ${idx + 1}`} className="h-full w-full object-contain" />
                                             </div>
                                         ))}
@@ -2175,7 +2220,7 @@ export default function POSPage() {
                     {posMode === 'new_sale' && (
                     <div className="border-t border-gray-100 pt-6 mt-6 space-y-4">
                         <div className="flex items-center justify-between">
-                            <h4 className="text-[10px] font-bold uppercase tracking-[0.2em] text-gray-400">Fecha de Entrega</h4>
+                            <h4 className="text-xs font-bold uppercase tracking-[0.2em] text-gray-400">Fecha de Entrega</h4>
                             <div className="flex items-center gap-4">
                                 <label className="inline-flex items-center cursor-pointer text-[9px] font-bold uppercase tracking-wider text-gray-500 gap-1.5 select-none">
                                     <input 
@@ -2223,11 +2268,11 @@ export default function POSPage() {
                                                 <span className="text-white text-lg">­ƒôà</span>
                                             </div>
                                             <div>
-                                                <p className="text-[10px] uppercase tracking-widest text-gray-400 mb-0.5">Entrega programada</p>
+                                                <p className="text-xs font-normal text-zinc-400 tracking-normal text-gray-400 mb-0.5">Entrega programada</p>
                                                 <p className="text-sm font-bold text-brand-charcoal capitalize">{fmt.day}</p>
-                                                <p className="text-xs font-semibold text-brand-terracotta">ÔÅ░ {fmt.time} hrs</p>
+                                                <p className="text-xs font-medium text-brand-terracotta">ÔÅ░ {fmt.time} hrs</p>
                                             </div>
-                                            <span className="ml-auto text-[10px] text-gray-400 uppercase tracking-wide">Editar ÔåÆ</span>
+                                            <span className="ml-auto text-xs text-gray-400 uppercase tracking-wide">Editar ÔåÆ</span>
                                         </div>
                                     );
                                 })() : (
@@ -2236,17 +2281,17 @@ export default function POSPage() {
                                             <span className="text-2xl">­ƒôà</span>
                                         </div>
                                         <div>
-                                            <p className="text-[10px] uppercase tracking-widest text-gray-400 mb-0.5">Fecha y hora de entrega</p>
+                                            <p className="text-xs font-normal text-zinc-400 tracking-normal text-gray-400 mb-0.5">Fecha y hora de entrega</p>
                                             <p className="text-sm text-gray-400 italic">Toca para seleccionar...</p>
                                         </div>
-                                        <span className="ml-auto text-[10px] text-brand-terracotta uppercase tracking-wide font-bold">Requerido ÔåÆ</span>
+                                        <span className="ml-auto text-xs text-brand-terracotta uppercase tracking-wide font-bold">Requerido ÔåÆ</span>
                                     </div>
                                 )}
                             </button>
 
                             {/* ÔöÇÔöÇ PICKER PANEL ÔöÇÔöÇ */}
                             {showDatePicker && (
-                                <div className="absolute bottom-full mb-2 left-0 right-0 bg-white rounded-xl shadow-2xl border border-gray-100 overflow-hidden z-50" style={{minWidth:'300px'}}>
+                                <div className="absolute bottom-full mb-2 left-0 right-0 bg-white rounded-2xl shadow-2xl border border-gray-100 overflow-hidden z-50" style={{minWidth:'300px'}}>
 
                                     {/* ÔöÇÔöÇ STEP 1: CALENDAR ÔöÇÔöÇ */}
                                     {pickerStep === 'date' && (
@@ -2259,7 +2304,7 @@ export default function POSPage() {
                                             </div>
                                             {/* Day headers */}
                                             <div className="grid grid-cols-7 bg-gray-50 border-b border-gray-100">
-                                                {DAY_NAMES.map(d => <div key={d} className="text-center py-2 text-[10px] font-bold uppercase tracking-wide text-gray-400">{d}</div>)}
+                                                {DAY_NAMES.map(d => <div key={d} className="text-center py-2 text-xs font-bold uppercase tracking-wide text-gray-400">{d}</div>)}
                                             </div>
                                             {/* Days grid */}
                                             <div className="grid grid-cols-7 p-2 gap-1">
@@ -2276,7 +2321,7 @@ export default function POSPage() {
                                                             type="button"
                                                             disabled={isPast}
                                                             onClick={() => selectDay(day)}
-                                                            className={`relative w-full aspect-square flex items-center justify-center rounded-lg text-sm font-semibold transition-all ${
+                                                            className={`relative w-full aspect-square flex items-center justify-center rounded-lg text-sm font-medium transition-all ${
                                                                 isPast ? 'text-gray-200 cursor-not-allowed' :
                                                                 isSelected ? 'bg-brand-terracotta text-white shadow-md scale-110' :
                                                                 isToday ? 'border-2 border-brand-terracotta text-brand-terracotta hover:bg-brand-terracotta/10' :
@@ -2289,7 +2334,7 @@ export default function POSPage() {
                                                 })}
                                             </div>
                                             <div className="px-4 pb-3 text-center">
-                                                <p className="text-[10px] text-gray-400">Selecciona el d├¡a de entrega</p>
+                                                <p className="text-xs text-gray-400">Selecciona el d├¡a de entrega</p>
                                             </div>
                                         </div>
                                     )}
@@ -2302,7 +2347,7 @@ export default function POSPage() {
                                                 <button type="button" onClick={() => setPickerStep('date')} className="w-8 h-8 flex items-center justify-center rounded-full hover:bg-white/10 text-white transition-all text-lg">ÔÇ╣</button>
                                                 <div>
                                                     <p className="text-white text-sm font-bold capitalize">{(() => { const d = new Date(tempDate + 'T12:00'); return d.toLocaleDateString('es-CL', { weekday: 'long', day: 'numeric', month: 'long' }); })()}</p>
-                                                    <p className="text-brand-terracotta text-[10px] uppercase tracking-widest">Selecciona la hora</p>
+                                                    <p className="text-brand-terracotta text-xs font-normal text-zinc-400 tracking-normal">Selecciona la hora</p>
                                                 </div>
                                             </div>
                                             {/* Time display */}
@@ -2372,7 +2417,7 @@ export default function POSPage() {
                                                     type="button" 
                                                     onClick={confirmTime}
                                                     disabled={isDateTimePast(tempDate, tempHour, tempMinute)}
-                                                    className="w-full py-3 bg-brand-charcoal text-white rounded-lg text-[10px] uppercase tracking-widest font-bold hover:bg-brand-terracotta transition-all disabled:opacity-50 disabled:cursor-not-allowed"
+                                                    className="w-full py-3 bg-brand-charcoal text-white rounded-lg text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-brand-terracotta transition-all disabled:opacity-50 disabled:cursor-not-allowed"
                                                 >
                                                     Confirmar Entrega {tempHour}:{tempMinute} hrs Ô£ô
                                                 </button>
@@ -2385,7 +2430,7 @@ export default function POSPage() {
                         </div>
 
                         {!deadline && (
-                            <p className="text-[10px] text-red-400 font-medium flex items-center gap-1">
+                            <p className="text-xs text-red-400 font-medium flex items-center gap-1">
                                 <span>ÔÜá</span> Debes ingresar la fecha y hora de entrega para continuar.
                             </p>
                         )}
@@ -2412,7 +2457,7 @@ export default function POSPage() {
                             
                             return (
                                 <div className="bg-brand-sand/10 border border-brand-sand/30 p-4 rounded-sm space-y-2 animate-in fade-in slide-in-from-top-1 duration-300">
-                                    <div className="flex justify-between items-center text-[10px] uppercase tracking-wider font-bold">
+                                    <div className="flex justify-between items-center text-xs uppercase tracking-wider font-bold">
                                         <span className="text-gray-500">Carga del Taller (Confecci├│n: {formattedWorkloadDate})</span>
                                         {loadingWorkload ? (
                                             <span className="text-brand-terracotta animate-pulse">Calculando...</span>
@@ -2434,7 +2479,7 @@ export default function POSPage() {
                                                     style={{ width: `${Math.min((combinedWorkloadHours / capacity) * 100, 100)}%` }}
                                                 />
                                             </div>
-                                            <p className={`text-[10px] italic font-medium ${
+                                            <p className={`text-xs italic font-medium ${
                                                 combinedWorkloadHours <= optimalThreshold ? 'text-green-700' :
                                                 combinedWorkloadHours <= intermediateThreshold ? 'text-amber-700' : 'text-rose-700'
                                             }`}>
@@ -2449,8 +2494,8 @@ export default function POSPage() {
                         })()}
 
                         {adjustedDates && (
-                            <div className="bg-gray-50 border border-gray-200/80 p-5 rounded-sm space-y-4 shadow-sm mt-4 animate-in fade-in duration-300">
-                                <h5 className="text-[10px] font-bold uppercase tracking-widest text-brand-terracotta flex items-center gap-1.5 border-b border-gray-200 pb-2">
+                            <div className="bg-gray-50 border border-zinc-100/80 p-5 rounded-sm space-y-4 shadow-[0_8px_30px_rgb(0,0,0,0.04)] mt-4 animate-in fade-in duration-300">
+                                <h5 className="text-xs font-medium text-zinc-600 tracking-normal text-brand-terracotta flex items-center gap-1.5 border-b border-zinc-100 pb-2">
                                     ÔÜÖ´©Å Desglose de Gobernanza de Tiempos
                                 </h5>
                                 <div className="space-y-3 text-xs">
@@ -2471,7 +2516,7 @@ export default function POSPage() {
                                         </strong>
                                     </div>
                                     <div className="flex justify-between items-center bg-brand-sand/20 p-2.5 rounded-sm border border-brand-sand/40">
-                                        <span className="text-brand-charcoal font-semibold">­ƒøì´©Å Retiro Cliente Prometido:</span>
+                                        <span className="text-brand-charcoal font-medium">­ƒøì´©Å Retiro Cliente Prometido:</span>
                                         <strong className="text-brand-terracotta font-serif text-sm">
                                             {new Date(adjustedDates.finalDeliveryDate).toLocaleDateString('es-CL', {
                                                 weekday: 'short', day: 'numeric', month: 'short'
@@ -2499,15 +2544,15 @@ export default function POSPage() {
 
                     {posMode === 'new_sale' && (
                     <div className="mt-6">
-                        <label className="block text-[10px] uppercase tracking-widest font-bold text-brand-charcoal mb-3">Abono Inicial</label>
+                        <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-brand-charcoal mb-3">Abono Inicial</label>
                         <div className="grid grid-cols-3 gap-3">
                             <button 
                                 type="button"
                                 disabled={hasUnassignedItems}
                                 onClick={() => setInitialPaymentType('total')}
-                                className={`py-3 text-[10px] uppercase tracking-widest font-bold transition-all rounded-sm border ${
-                                    hasUnassignedItems ? 'bg-gray-50 text-gray-300 border-gray-200 opacity-50 cursor-not-allowed' :
-                                    initialPaymentType === 'total' ? 'bg-brand-terracotta text-white border-brand-terracotta shadow-sm' : 'bg-white text-gray-500 border-gray-200 hover:border-brand-terracotta'
+                                className={`py-3 text-xs font-normal text-zinc-400 tracking-normal font-bold transition-all rounded-sm border ${
+                                    hasUnassignedItems ? 'bg-gray-50 text-gray-300 border-zinc-100 opacity-50 cursor-not-allowed' :
+                                    initialPaymentType === 'total' ? 'bg-brand-terracotta text-white border-brand-terracotta shadow-[0_8px_30px_rgb(0,0,0,0.04)]' : 'bg-white text-gray-500 border-zinc-100 hover:border-brand-terracotta'
                                 }`}
                             >
                                 Pago Total
@@ -2516,9 +2561,9 @@ export default function POSPage() {
                                 type="button"
                                 disabled={hasUnassignedItems}
                                 onClick={() => setInitialPaymentType('50percent')}
-                                className={`py-3 text-[10px] uppercase tracking-widest font-bold transition-all rounded-sm border ${
-                                    hasUnassignedItems ? 'bg-gray-50 text-gray-300 border-gray-200 opacity-50 cursor-not-allowed' :
-                                    initialPaymentType === '50percent' ? 'bg-brand-terracotta text-white border-brand-terracotta shadow-sm' : 'bg-white text-gray-500 border-gray-200 hover:border-brand-terracotta'
+                                className={`py-3 text-xs font-normal text-zinc-400 tracking-normal font-bold transition-all rounded-sm border ${
+                                    hasUnassignedItems ? 'bg-gray-50 text-gray-300 border-zinc-100 opacity-50 cursor-not-allowed' :
+                                    initialPaymentType === '50percent' ? 'bg-brand-terracotta text-white border-brand-terracotta shadow-[0_8px_30px_rgb(0,0,0,0.04)]' : 'bg-white text-gray-500 border-zinc-100 hover:border-brand-terracotta'
                                 }`}
                             >
                                 Abono 50%
@@ -2530,9 +2575,9 @@ export default function POSPage() {
                                     setInitialPaymentType('zero');
                                     setPaymentMethod(null);
                                 }}
-                                className={`py-3 text-[10px] uppercase tracking-widest font-bold transition-all rounded-sm border ${
-                                    hasUnassignedItems ? 'bg-gray-50 text-gray-300 border-gray-200 opacity-50 cursor-not-allowed' :
-                                    initialPaymentType === 'zero' ? 'bg-brand-terracotta text-white border-brand-terracotta shadow-sm' : 'bg-white text-gray-500 border-gray-200 hover:border-brand-terracotta'
+                                className={`py-3 text-xs font-normal text-zinc-400 tracking-normal font-bold transition-all rounded-sm border ${
+                                    hasUnassignedItems ? 'bg-gray-50 text-gray-300 border-zinc-100 opacity-50 cursor-not-allowed' :
+                                    initialPaymentType === 'zero' ? 'bg-brand-terracotta text-white border-brand-terracotta shadow-[0_8px_30px_rgb(0,0,0,0.04)]' : 'bg-white text-gray-500 border-zinc-100 hover:border-brand-terracotta'
                                 }`}
                             >
                                 Contra Entrega
@@ -2543,7 +2588,7 @@ export default function POSPage() {
 
                     {initialPaymentType !== 'zero' && (
                     <div className="mt-6">
-                    <label className="block text-[10px] uppercase tracking-widest font-bold text-brand-charcoal mb-3">M├®todo de Pago</label>
+                    <label className="block text-xs font-normal text-zinc-400 tracking-normal font-bold text-brand-charcoal mb-3">M├®todo de Pago</label>
                     <div className="grid grid-cols-3 gap-3">
                         <button 
                             type="button"
@@ -2551,10 +2596,10 @@ export default function POSPage() {
                             onClick={() => setPaymentMethod('mercadopago_point')}
                             className={`flex flex-col items-center justify-center gap-2 py-3.5 text-[9px] uppercase tracking-widest transition-all rounded-sm border ${
                                 hasUnassignedItems
-                                    ? 'bg-gray-50 text-gray-300 border-gray-200 cursor-not-allowed opacity-50'
+                                    ? 'bg-gray-50 text-gray-300 border-zinc-100 cursor-not-allowed opacity-50'
                                     : paymentMethod === 'mercadopago_point'
-                                        ? 'bg-brand-charcoal text-white border-brand-charcoal shadow-sm font-bold'
-                                        : 'border-gray-200 hover:border-brand-charcoal text-brand-charcoal bg-white cursor-pointer font-bold'
+                                        ? 'bg-brand-charcoal text-white border-brand-charcoal shadow-[0_8px_30px_rgb(0,0,0,0.04)] font-bold'
+                                        : 'border-zinc-100 hover:border-brand-charcoal text-brand-charcoal bg-white cursor-pointer font-bold'
                             }`}>
                             <CreditCard className="w-4 h-4 text-brand-terracotta" />
                             Pago M├íquina
@@ -2565,10 +2610,10 @@ export default function POSPage() {
                             onClick={() => setPaymentMethod('transbank')}
                             className={`flex flex-col items-center justify-center gap-2 py-3.5 text-[9px] uppercase tracking-widest transition-all rounded-sm border ${
                                 hasUnassignedItems
-                                    ? 'bg-gray-50 text-gray-300 border-gray-200 cursor-not-allowed opacity-50'
+                                    ? 'bg-gray-50 text-gray-300 border-zinc-100 cursor-not-allowed opacity-50'
                                     : paymentMethod === 'transbank'
-                                        ? 'bg-brand-charcoal text-white border-brand-charcoal shadow-sm font-bold'
-                                        : 'border-gray-200 hover:border-brand-charcoal text-brand-charcoal bg-white cursor-pointer font-bold'
+                                        ? 'bg-brand-charcoal text-white border-brand-charcoal shadow-[0_8px_30px_rgb(0,0,0,0.04)] font-bold'
+                                        : 'border-zinc-100 hover:border-brand-charcoal text-brand-charcoal bg-white cursor-pointer font-bold'
                             }`}>
                             <Globe className="w-4 h-4 text-blue-500" />
                             Pago en l├¡nea
@@ -2584,10 +2629,10 @@ export default function POSPage() {
                             }}
                             className={`flex flex-col items-center justify-center gap-2 py-3.5 text-[9px] uppercase tracking-widest transition-all rounded-sm border ${
                                 hasUnassignedItems
-                                    ? 'bg-gray-50 text-gray-300 border-gray-200 cursor-not-allowed opacity-50'
+                                    ? 'bg-gray-50 text-gray-300 border-zinc-100 cursor-not-allowed opacity-50'
                                     : paymentMethod === 'cash'
-                                        ? 'bg-brand-charcoal text-white border-brand-charcoal shadow-sm font-bold'
-                                        : 'border-gray-200 hover:border-brand-charcoal text-brand-charcoal bg-white cursor-pointer font-bold'
+                                        ? 'bg-brand-charcoal text-white border-brand-charcoal shadow-[0_8px_30px_rgb(0,0,0,0.04)] font-bold'
+                                        : 'border-zinc-100 hover:border-brand-charcoal text-brand-charcoal bg-white cursor-pointer font-bold'
                             }`}>
                             <span className="text-[14px] leading-none flex gap-1 justify-center w-full">­ƒÆÁ ­ƒÆ│</span>
                             <span className="text-center px-1">Efectivo / Mixto</span>
@@ -2598,7 +2643,7 @@ export default function POSPage() {
 
                     {paymentMethod === 'cash' && initialPaymentType !== 'zero' && (
                         <div className="mt-4 p-4 border border-brand-terracotta/20 bg-brand-terracotta/5 rounded-sm space-y-4">
-                            <h4 className="text-[10px] font-bold uppercase tracking-widest text-brand-charcoal text-center mb-2">Detalle de Pago</h4>
+                            <h4 className="text-xs font-medium text-zinc-600 tracking-normal text-brand-charcoal text-center mb-2">Detalle de Pago</h4>
                             <div className="grid grid-cols-2 gap-4">
                                 <div>
                                     <label className="block text-[9px] uppercase tracking-widest text-gray-500 font-bold mb-1">Monto Tarjeta (M├íquina)</label>
@@ -2634,7 +2679,7 @@ export default function POSPage() {
                                 </div>
                             </div>
                             {splitCardAmount + splitCashAmount !== (initialPaymentType === '50percent' ? Math.round(total / 2) : total) && (
-                                <p className="text-[10px] text-red-500 font-bold text-center">ÔÜá La suma debe ser exactamente {formatCurrency(initialPaymentType === '50percent' ? Math.round(total / 2) : total)}</p>
+                                <p className="text-xs text-red-500 font-bold text-center">ÔÜá La suma debe ser exactamente {formatCurrency(initialPaymentType === '50percent' ? Math.round(total / 2) : total)}</p>
                             )}
                         </div>
                     )}
@@ -2646,7 +2691,7 @@ export default function POSPage() {
                                 type="button"
                                 onClick={generateBudgetLink}
                                 disabled={cart.length === 0}
-                                className="w-full border border-brand-charcoal text-brand-charcoal py-4 text-[10px] uppercase tracking-widest font-bold hover:bg-gray-50 transition-all disabled:opacity-50 disabled:cursor-not-allowed">
+                                className="w-full border border-brand-charcoal text-brand-charcoal py-4 text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-gray-50 transition-all disabled:opacity-50 disabled:cursor-not-allowed">
                                 Generar Presupuesto Web (Link)
                             </button>
                         )}
@@ -2654,7 +2699,7 @@ export default function POSPage() {
                             type="button"
                             onClick={handleCheckout}
                             disabled={cart.length === 0 || !selectedCustomer || (posMode === 'new_sale' && !deadline) || isProcessing || hasUnassignedItems || (initialPaymentType !== 'zero' && (!paymentMethod || (paymentMethod === 'split' && splitCardAmount + splitCashAmount !== (initialPaymentType === '50percent' ? Math.round(total / 2) : total))))}
-                            className={`w-full py-4 text-[10px] uppercase tracking-widest font-bold transition-all ${
+                            className={`w-full py-4 text-xs font-normal text-zinc-400 tracking-normal font-bold transition-all ${
                                 cart.length === 0 || !selectedCustomer || (posMode === 'new_sale' && !deadline) || isProcessing || hasUnassignedItems || (initialPaymentType !== 'zero' && (!paymentMethod || (paymentMethod === 'split' && splitCardAmount + splitCashAmount !== (initialPaymentType === '50percent' ? Math.round(total / 2) : total))))
                                     ? 'bg-gray-200 text-gray-500 cursor-not-allowed'
                                     : 'bg-green-600 text-white hover:bg-green-700 shadow-md'
@@ -2689,7 +2734,7 @@ export default function POSPage() {
                                     setSplitCardAmount(0);
                                     setSplitCashAmount(0);
                                 }}
-                                className="w-full border border-red-200 text-red-500 py-3 text-[10px] uppercase tracking-widest font-bold hover:bg-red-50 hover:border-red-500 transition-all mt-2">
+                                className="w-full border border-red-200 text-red-500 py-3 text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-red-50 hover:border-red-500 transition-all mt-2">
                                 {posMode === 'pay_balance' ? 'Cancelar Pago de Saldo' : 'Cancelar Venta y Vaciar Carrito'}
                             </button>
                         )}
@@ -2702,7 +2747,7 @@ export default function POSPage() {
             {isBudgetModalOpen && (
                 <div className="fixed inset-0 bg-black/50 z-50 flex items-center justify-center p-4">
                     <div className="bg-white rounded-sm shadow-xl w-full max-w-lg flex flex-col overflow-hidden animate-in zoom-in duration-300">
-                        <div className="p-8 text-center space-y-6">
+                        <div className="p-8 text-center space-y-8">
                             <div className="w-16 h-16 bg-brand-sand/30 rounded-full flex items-center justify-center mx-auto">
                                 <Plus className="w-8 h-8 text-brand-terracotta rotate-45" />
                             </div>
@@ -2714,39 +2759,39 @@ export default function POSPage() {
                             <div className="bg-gray-50 p-4 rounded-sm border border-gray-100 flex flex-col gap-4">
                                 <div className="space-y-4">
                                     <div className="flex flex-col gap-2">
-                                        <p className="text-[10px] uppercase tracking-widest font-bold text-gray-400 text-left italic">Enviar por WhatsApp</p>
+                                        <p className="text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-400 text-left italic">Enviar por WhatsApp</p>
                                         <div className="flex gap-2">
                                             <input 
                                                 type="tel" 
                                                 value={clientPhone}
                                                 onChange={(e) => setClientPhone(e.target.value)}
                                                 placeholder="Ej. 56912345678" 
-                                                className="flex-1 bg-white border border-gray-200 px-3 py-2 text-xs rounded-sm outline-none focus:border-brand-terracotta" 
+                                                className="flex-1 bg-white border border-zinc-100 px-3 py-2 text-xs rounded-sm outline-none focus:border-brand-terracotta" 
                                             />
                                             <button 
                                                 onClick={shareViaWhatsApp}
                                                 disabled={!clientPhone}
-                                                className="bg-[#25D366] text-white px-4 py-2 text-[10px] uppercase tracking-widest font-bold hover:bg-[#128C7E] transition-all rounded-sm flex items-center gap-2 disabled:opacity-50"
+                                                className="bg-[#25D366] text-white px-4 py-2 text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-[#128C7E] transition-all rounded-sm flex items-center gap-2 disabled:opacity-50"
                                             >
                                                 <MessageSquare className="w-3 h-3" /> WhatsApp
                                             </button>
                                         </div>
                                     </div>
 
-                                    <div className="flex flex-col gap-2 border-t border-gray-200 pt-4">
-                                        <p className="text-[10px] uppercase tracking-widest font-bold text-gray-400 text-left italic">Enviar por Correo</p>
+                                    <div className="flex flex-col gap-2 border-t border-zinc-100 pt-4">
+                                        <p className="text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-400 text-left italic">Enviar por Correo</p>
                                         <div className="flex gap-2">
                                             <input 
                                                 type="email" 
                                                 value={clientEmail}
                                                 onChange={(e) => setClientEmail(e.target.value)}
                                                 placeholder="cliente@email.com" 
-                                                className="flex-1 bg-white border border-gray-200 px-3 py-2 text-xs rounded-sm outline-none focus:border-brand-terracotta" 
+                                                className="flex-1 bg-white border border-zinc-100 px-3 py-2 text-xs rounded-sm outline-none focus:border-brand-terracotta" 
                                             />
                                             <button 
                                                 onClick={shareViaEmail}
                                                 disabled={!clientEmail || isSendingEmail}
-                                                className="bg-brand-charcoal text-white px-4 py-2 text-[10px] uppercase tracking-widest font-bold hover:bg-brand-terracotta transition-all rounded-sm flex items-center gap-2 disabled:opacity-50 min-w-[100px] justify-center"
+                                                className="bg-brand-charcoal text-white px-4 py-2 text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-brand-terracotta transition-all rounded-sm flex items-center gap-2 disabled:opacity-50 min-w-[100px] justify-center"
                                             >
                                                 {isSendingEmail ? (
                                                     <Loader2 className="w-3 h-3 animate-spin text-brand-sand" />
@@ -2759,18 +2804,18 @@ export default function POSPage() {
                                     </div>
                                 </div>
                                 
-                                <div className="border-t border-gray-200 pt-4">
-                                    <p className="text-[10px] uppercase tracking-widest font-bold text-gray-400 text-left mb-2">Link Directo</p>
+                                <div className="border-t border-zinc-100 pt-4">
+                                    <p className="text-xs font-normal text-zinc-400 tracking-normal font-bold text-gray-400 text-left mb-2">Link Directo</p>
                                     <div className="flex gap-2">
                                         <input 
                                             type="text" 
                                             readOnly 
                                             value={generatedLink} 
-                                            className="flex-1 bg-white border border-gray-200 px-3 py-2 text-[10px] text-gray-400 rounded-sm outline-none" 
+                                            className="flex-1 bg-white border border-zinc-100 px-3 py-2 text-xs text-gray-400 rounded-sm outline-none text-base min-h-[44px]" 
                                         />
                                         <button 
                                             onClick={copyToClipboard}
-                                            className={`px-4 py-2 text-[10px] uppercase tracking-widest font-bold transition-all rounded-sm flex items-center gap-2 ${copySuccess ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-600 hover:bg-gray-300'}`}
+                                            className={`px-4 py-2 text-xs font-normal text-zinc-400 tracking-normal font-bold transition-all rounded-sm flex items-center gap-2 ${copySuccess ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-600 hover:bg-gray-300'}`}
                                         >
                                             {copySuccess ? 'Copiado' : 'Copiar Link'}
                                         </button>
@@ -2781,13 +2826,13 @@ export default function POSPage() {
                             <div className="pt-4 grid grid-cols-2 gap-4">
                                 <button 
                                     onClick={() => window.open(generatedLink, '_blank')}
-                                    className="flex items-center justify-center gap-2 py-3 border border-gray-200 text-brand-charcoal text-[10px] uppercase tracking-widest font-bold hover:bg-gray-50 transition-all rounded-sm"
+                                    className="flex items-center justify-center gap-2 py-3 border border-zinc-100 text-brand-charcoal text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-gray-50 transition-all rounded-sm"
                                 >
                                     Ver como Cliente
                                 </button>
                                 <button 
                                     onClick={handleCloseBudgetModal}
-                                    className="py-3 bg-brand-charcoal text-white text-[10px] uppercase tracking-widest font-bold hover:bg-brand-terracotta transition-all rounded-sm"
+                                    className="py-3 bg-brand-charcoal text-white text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-brand-terracotta transition-all rounded-sm"
                                 >
                                     Finalizar
                                 </button>
@@ -2806,7 +2851,7 @@ export default function POSPage() {
                             paymentConfirmed={paymentConfirmed} 
                             setPaymentConfirmed={setPaymentConfirmed} 
                         />
-                        <div className="bg-brand-charcoal text-white p-6 flex justify-between items-center">
+                        <div className="bg-brand-charcoal text-white p-8 flex justify-between items-center">
                             <div className="flex items-center gap-3">
                                 <ClipboardList className="w-6 h-6 text-brand-sand" />
                                 <div>
@@ -2821,20 +2866,20 @@ export default function POSPage() {
                             {/* Staff Info */}
                             <div className="grid grid-cols-2 gap-8 pb-6 border-b border-gray-100">
                                 <div>
-                                    <p className="text-[10px] uppercase tracking-widest text-gray-400 mb-1">Cliente</p>
+                                    <p className="text-xs font-normal text-zinc-400 tracking-normal text-gray-400 mb-1">Cliente</p>
                                     <p className="font-serif text-lg">{checkoutResult.customer.full_name}</p>
                                     <p className="text-xs text-gray-500">{checkoutResult.customer.phone || 'Sin tel├®fono'}</p>
                                 </div>
                                 <div className="text-right">
-                                    <p className="text-[10px] uppercase tracking-widest text-gray-400 mb-1">Fecha de Ingreso</p>
+                                    <p className="text-xs font-normal text-zinc-400 tracking-normal text-gray-400 mb-1">Fecha de Ingreso</p>
                                     <p className="text-sm font-bold">{checkoutResult.date}</p>
                                     {checkoutResult.deliveryDate && (
                                         <div className="mt-2">
-                                            <p className="text-[10px] uppercase tracking-widest text-brand-terracotta mb-1">Fecha Estimada Entrega</p>
+                                            <p className="text-xs font-normal text-zinc-400 tracking-normal text-brand-terracotta mb-1">Fecha Estimada Entrega</p>
                                             <p className="text-sm font-bold">{checkoutResult.deliveryDate}</p>
                                         </div>
                                     )}
-                                    <p className="text-[10px] text-brand-terracotta font-bold uppercase mt-3">Metodo: {checkoutResult.method === 'mercadopago_point' ? 'Mercado Pago Point' : checkoutResult.method === 'transbank' ? 'Webpay Plus' : checkoutResult.method === 'cash' ? 'Efectivo / Transferencia' : checkoutResult.method}</p>
+                                    <p className="text-xs text-brand-terracotta font-bold uppercase mt-3">Metodo: {checkoutResult.method === 'mercadopago_point' ? 'Mercado Pago Point' : checkoutResult.method === 'transbank' ? 'Webpay Plus' : checkoutResult.method === 'cash' ? 'Efectivo / Transferencia' : checkoutResult.method}</p>
                                 </div>
                             </div>
 
@@ -2846,10 +2891,10 @@ export default function POSPage() {
                                         <div key={idx} className="bg-gray-50 p-4 rounded-sm border border-gray-100">
                                             <div className="flex justify-between items-start mb-2">
                                                 <p className="font-serif text-md">{item.name}</p>
-                                                <span className="text-[10px] uppercase font-bold text-brand-terracotta">{item.category}</span>
+                                                <span className="text-xs uppercase font-bold text-brand-terracotta">{item.category}</span>
                                             </div>
                                             {item.isCustom && item.details && (
-                                                <div className="grid grid-cols-3 gap-4 mt-2 pt-2 border-t border-gray-200/50">
+                                                <div className="grid grid-cols-3 gap-4 mt-2 pt-2 border-t border-zinc-100/50">
                                                     <div className="col-span-1">
                                                         <p className="text-[9px] uppercase text-gray-400">Horas Estimadas</p>
                                                         <p className="text-xs font-bold">{item.details.hours} hrs</p>
@@ -2869,7 +2914,7 @@ export default function POSPage() {
                                                     <p className="text-[9px] uppercase tracking-widest font-bold text-brand-terracotta">Registro Fotogr├ífico ({item.images.length})</p>
                                                     <div className="grid grid-cols-2 gap-3">
                                                         {item.images.map((img: any, imgIdx: number) => (
-                                                            <div key={imgIdx} className="space-y-1 bg-white p-2 border border-gray-100 rounded-sm shadow-sm">
+                                                            <div key={imgIdx} className="space-y-1 bg-white p-2 border border-gray-100 rounded-sm shadow-[0_8px_30px_rgb(0,0,0,0.04)]">
                                                                 <div className="border border-gray-100 rounded-[1px] overflow-hidden bg-gray-50 flex items-center justify-center h-[90px]">
                                                                     <img src={img.url} alt={`Registro ${imgIdx + 1}`} className="h-full w-full object-contain" />
                                                                 </div>
@@ -2889,13 +2934,13 @@ export default function POSPage() {
                             </div>
 
                             <div className="bg-brand-sand/10 p-4 rounded-sm border border-brand-sand/30 text-center">
-                                <p className="text-[10px] uppercase tracking-widest text-brand-charcoal mb-1">Total de la Orden</p>
+                                <p className="text-xs font-normal text-zinc-400 tracking-normal text-brand-charcoal mb-1">Total de la Orden</p>
                                 <p className="text-3xl font-serif text-brand-terracotta">{formatCurrency(checkoutResult.total)}</p>
                             </div>
 
                             {!((checkoutResult.method === 'mercadopago_point' || checkoutResult.method?.startsWith('Mixto')) && !paymentConfirmed) && (
                             <div className="border-t border-gray-100 pt-6 space-y-4 print:hidden">
-                                <div className="bg-gray-50 p-4 rounded-sm border border-gray-200/60 flex flex-col md:flex-row gap-4 items-center justify-between">
+                                <div className="bg-gray-50 p-4 rounded-sm border border-zinc-100/60 flex flex-col md:flex-row gap-4 items-center justify-between">
                                     <div className="text-left">
                                         <p className="text-[9px] uppercase tracking-widest font-bold text-gray-400">Notificaciones al Cliente</p>
                                         <p className="text-xs text-gray-500 font-medium">Reenviar comprobante a: {checkoutResult.customer.email || 'Sin correo'}</p>
@@ -2972,13 +3017,13 @@ export default function POSPage() {
                             )}
                         </div>
 
-                        <div className="p-6 bg-gray-50 border-t border-gray-100 flex flex-col sm:flex-row gap-4">
+                        <div className="p-8 bg-gray-50 border-t border-gray-100 flex flex-col sm:flex-row gap-4">
                             {checkoutResult.paymentUrl && checkoutResult.method === 'transbank' && (
                                 <a 
                                     href={checkoutResult.paymentUrl}
                                     target="_blank"
                                     rel="noopener noreferrer"
-                                    className="flex-1 py-3 bg-brand-terracotta hover:bg-brand-charcoal text-white text-[10px] uppercase tracking-widest font-bold transition-all rounded-sm text-center flex items-center justify-center gap-2 shadow-md"
+                                    className="flex-1 py-3 bg-brand-terracotta hover:bg-brand-charcoal text-white text-xs tracking-wider font-bold transition-all rounded-sm text-center flex items-center justify-center gap-2 shadow-md"
                                 >
                                     <span>­ƒÆ│</span> Ir a P├ígina de Pago
                                 </a>
@@ -2986,12 +3031,12 @@ export default function POSPage() {
                             
                             {((checkoutResult.method === 'mercadopago_point' || checkoutResult.method?.startsWith('Mixto')) && !paymentConfirmed) ? (
                                 <div className="flex-1 flex flex-col sm:flex-row gap-2">
-                                    <div className="flex-1 py-3 bg-gray-100 text-brand-charcoal text-[10px] uppercase tracking-widest font-bold transition-all rounded-sm text-center flex flex-col items-center justify-center gap-1 opacity-70">
+                                    <div className="flex-1 py-3 bg-gray-100 text-brand-charcoal text-xs tracking-wider font-bold transition-all rounded-sm text-center flex flex-col items-center justify-center gap-1 opacity-70">
                                         <div className="flex items-center gap-2"><Loader2 className="w-3.5 h-3.5 animate-spin" /> Esperando Webhook...</div>
                                     </div>
                                     <button 
                                         onClick={() => setPaymentConfirmed(true)} 
-                                        className="flex-1 py-3 bg-brand-terracotta text-white text-[10px] uppercase tracking-widest font-bold hover:bg-brand-charcoal transition-all rounded-sm shadow-md"
+                                        className="flex-1 py-3 bg-brand-terracotta text-white text-xs tracking-wider font-bold hover:bg-brand-charcoal transition-all rounded-sm shadow-md"
                                     >
                                         Validar Manualmente
                                     </button>
@@ -3007,18 +3052,18 @@ export default function POSPage() {
                                                 }
                                             }
                                         }}
-                                        className="flex-1 py-3 bg-red-100 text-red-600 border border-red-300 text-[10px] uppercase tracking-widest font-bold hover:bg-red-200 transition-all rounded-sm shadow-md"
+                                        className="flex-1 py-3 bg-red-100 text-red-600 border border-red-300 text-xs tracking-wider font-bold hover:bg-red-200 transition-all rounded-sm shadow-md"
                                     >
                                         Anular Operaci├│n Fallida
                                     </button>
                                 </div>
                             ) : (
-                                <button onClick={() => window.print()} className="flex-1 py-3 border border-brand-charcoal text-brand-charcoal text-[10px] uppercase tracking-widest font-bold hover:bg-white transition-all rounded-sm">
+                                <button onClick={() => window.print()} className="flex-1 py-3 border border-brand-charcoal text-brand-charcoal text-xs tracking-wider font-bold hover:bg-white transition-all rounded-sm">
                                     {(checkoutResult.method === 'mercadopago_point' || checkoutResult.method?.startsWith('Mixto')) ? 'Ô£à Imprimir (Aprobado)' : 'Imprimir Orden (Taller)'}
                                 </button>
                             )}
                             
-                            <button onClick={handleCloseCheckout} className="flex-1 py-3 bg-brand-charcoal text-white text-[10px] uppercase tracking-widest font-bold hover:bg-brand-terracotta transition-all rounded-sm">
+                            <button onClick={handleCloseCheckout} className="flex-1 py-3 bg-brand-charcoal text-white text-xs tracking-wider font-bold hover:bg-brand-terracotta transition-all rounded-sm">
                                 Finalizar y Nueva Orden
                             </button>
                         </div>
@@ -3030,14 +3075,14 @@ export default function POSPage() {
             {showAuthModal && (
                 <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm animate-in fade-in duration-200">
                     <div className="bg-white max-w-sm w-full shadow-2xl rounded-sm overflow-hidden animate-in zoom-in-95 duration-300">
-                        <div className="bg-[#f8d7da] p-6 text-center border-b border-[#f5c6cb]">
-                            <div className="mx-auto w-12 h-12 bg-white rounded-full flex items-center justify-center mb-3 shadow-sm">
+                        <div className="bg-[#f8d7da] p-8 text-center border-b border-[#f5c6cb]">
+                            <div className="mx-auto w-12 h-12 bg-white rounded-full flex items-center justify-center mb-3 shadow-[0_8px_30px_rgb(0,0,0,0.04)]">
                                 <AlertCircle className="w-6 h-6 text-[#721c24]" />
                             </div>
                             <h2 className="text-lg font-bold text-[#721c24] mb-1">Autorizaci├│n Requerida</h2>
                             <p className="text-xs text-[#721c24]/80">Descuento superior al 20%</p>
                         </div>
-                        <div className="p-6">
+                        <div className="p-8">
                             {isAuthorizing ? (
                                 <div className="flex flex-col items-center justify-center py-6">
                                     <Loader2 className="w-8 h-8 animate-spin text-brand-terracotta mb-4" />
@@ -3053,7 +3098,7 @@ export default function POSPage() {
                                         maxLength={4}
                                         value={authPinInput}
                                         onChange={(e) => setAuthPinInput(e.target.value.replace(/\D/g, ''))}
-                                        className="w-full text-center text-3xl font-bold tracking-[0.5em] p-4 bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta"
+                                        className="w-full text-center text-3xl font-bold tracking-[0.5em] p-4 bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta"
                                         placeholder="ÔÇóÔÇóÔÇóÔÇó"
                                         autoFocus
                                     />
@@ -3065,7 +3110,7 @@ export default function POSPage() {
                                                 setAuthPinInput('');
                                                 setExpectedPin('');
                                             }} 
-                                            className="flex-1 py-3 border border-gray-200 text-gray-600 text-[10px] uppercase tracking-widest font-bold hover:bg-gray-50 transition-all rounded-sm"
+                                            className="flex-1 py-3 border border-zinc-100 text-gray-600 text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-gray-50 transition-all rounded-sm"
                                         >
                                             Cancelar
                                         </button>
@@ -3084,7 +3129,7 @@ export default function POSPage() {
                                                 }
                                             }}
                                             disabled={authPinInput.length !== 4}
-                                            className="flex-1 py-3 bg-brand-terracotta text-white text-[10px] uppercase tracking-widest font-bold hover:bg-brand-charcoal transition-all rounded-sm disabled:opacity-50"
+                                            className="flex-1 py-3 bg-brand-terracotta text-white text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-brand-charcoal transition-all rounded-sm disabled:opacity-50"
                                         >
                                             Autorizar
                                         </button>
@@ -3099,7 +3144,7 @@ export default function POSPage() {
             {loadBalancerModal && loadBalancerModal.show && (
                 <div className="fixed inset-0 bg-black/60 backdrop-blur-sm z-50 flex items-center justify-center p-4">
                     <div className="bg-white max-w-lg w-full rounded-sm shadow-2xl overflow-hidden border-t-4 border-red-500">
-                        <div className="p-6 bg-red-50 border-b border-red-100 flex items-start gap-4">
+                        <div className="p-8 bg-red-50 border-b border-red-100 flex items-start gap-4">
                             <AlertCircle className="w-8 h-8 text-red-500 shrink-0 mt-1" />
                             <div>
                                 <h3 className="text-lg font-bold text-red-800">Alerta de Sobrecarga</h3>
@@ -3110,13 +3155,13 @@ export default function POSPage() {
                             </div>
                         </div>
                         
-                        <div className="p-6 space-y-4">
+                        <div className="p-8 space-y-4">
                             <h4 className="text-xs font-bold uppercase tracking-widest text-brand-charcoal border-b border-gray-100 pb-2">Sugerencias con capacidad inmediata:</h4>
                             
                             {loadBalancerModal.alternatives.length > 0 ? (
                                 <div className="space-y-3">
                                     {loadBalancerModal.alternatives.map((alt: any) => (
-                                        <div key={alt.id} className="flex items-center justify-between p-3 bg-gray-50 border border-gray-200 rounded-sm hover:border-brand-sand transition-colors">
+                                        <div key={alt.id} className="flex items-center justify-between p-3 bg-gray-50 border border-zinc-100 rounded-sm hover:border-brand-sand transition-colors">
                                             <div>
                                                 <p className="font-bold text-brand-charcoal">{alt.name}</p>
                                                 <p className="text-xs text-emerald-600 font-medium">Carga actual: {alt.loadDays} d├¡as ({alt.workloadPercentage}%)</p>
@@ -3133,7 +3178,7 @@ export default function POSPage() {
                                                     }
                                                     setLoadBalancerModal(null);
                                                 }}
-                                                className="px-4 py-2 bg-brand-charcoal text-white text-[10px] uppercase font-bold tracking-wider rounded-sm hover:bg-brand-terracotta transition-colors"
+                                                className="px-4 py-2 bg-brand-charcoal text-white text-xs uppercase font-bold tracking-wider rounded-sm hover:bg-brand-terracotta transition-colors"
                                             >
                                                 Reasignar
                                             </button>
@@ -3164,7 +3209,7 @@ export default function POSPage() {
                                     }
                                     setLoadBalancerModal(null);
                                 }}
-                                className="px-4 py-2 bg-red-100 text-red-700 text-[10px] font-bold uppercase tracking-wider rounded-sm hover:bg-red-200 transition-colors"
+                                className="px-4 py-2 bg-red-100 text-red-700 text-xs font-bold uppercase tracking-wider rounded-sm hover:bg-red-200 transition-colors"
                             >
                                 Forzar Asignaci├│n (Exclusivo)
                             </button>
@@ -3176,9 +3221,9 @@ export default function POSPage() {
             {/* Haute Couture Calculator Modal */}
             {isHauteCoutureModalOpen && (
                 <div className="fixed inset-0 bg-black/70 backdrop-blur-md z-50 flex items-center justify-center p-2 md:p-4 overflow-y-auto animate-in fade-in duration-300">
-                    <div className="bg-white text-brand-charcoal rounded-sm border border-gray-200 shadow-2xl w-full max-w-5xl flex flex-col overflow-hidden max-h-[95vh]">
+                    <div className="bg-white text-brand-charcoal rounded-sm border border-zinc-100 shadow-2xl w-full max-w-5xl flex flex-col overflow-hidden max-h-[95vh]">
                         {/* Header */}
-                        <div className="p-4 md:p-6 border-b border-gray-100 flex justify-between items-center bg-brand-sand/10 shrink-0">
+                        <div className="p-4 md:p-8 border-b border-gray-100 flex justify-between items-center bg-brand-sand/10 shrink-0">
                             <div>
                                 <h2 className="font-serif text-xl md:text-2xl text-brand-charcoal tracking-wide">Calculadora Alta Costura</h2>
                                 <p className="text-xs text-gray-500 mt-1">Desglose t├®cnico de costos y tiempos{customOrderName ? `: ${customOrderName}` : ''}</p>
@@ -3190,23 +3235,23 @@ export default function POSPage() {
 
                         {/* Templates Carousel */}
                         <div className="bg-brand-sand/5 border-b border-gray-100 p-4 shrink-0">
-                            <span className="block text-[10px] uppercase tracking-wider text-brand-terracotta mb-3 font-semibold">Siluetas Base del Taller</span>
+                            <span className="block text-xs uppercase tracking-wider text-brand-terracotta mb-3 font-medium">Siluetas Base del Taller</span>
                             <div className="flex gap-3 overflow-x-auto pb-2 scrollbar-thin">
                                 {hcTemplates.map((tpl) => (
                                     <div
                                         key={tpl.id}
                                         onClick={() => handleApplyTemplate(tpl)}
-                                        className={`relative bg-white border rounded-md p-3 min-w-[120px] w-[120px] text-center cursor-pointer transition-all group flex flex-col items-center justify-between shadow-sm min-h-[110px] ${
+                                        className={`relative bg-white border rounded-md p-3 min-w-[120px] w-[120px] text-center cursor-pointer transition-all group flex flex-col items-center justify-between shadow-[0_8px_30px_rgb(0,0,0,0.04)] min-h-[110px] ${
                                             hcPrendaName === tpl.name
                                                 ? 'border-brand-terracotta bg-brand-sand/25 ring-1 ring-brand-terracotta/40'
-                                                : 'border-gray-200 hover:border-brand-charcoal hover:bg-neutral-50'
+                                                : 'border-zinc-100 hover:border-brand-charcoal hover:bg-neutral-50'
                                         }`}
                                     >
                                         <div 
                                             className="h-16 w-full flex items-center justify-center mb-1.5 text-brand-charcoal hover:text-brand-terracotta transition-colors"
                                             dangerouslySetInnerHTML={{ __html: tpl.svg || tpl.img || `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="w-8 h-8"><rect x="3" y="3" width="18" height="18" rx="2" /><path d="M21 15l-4-4-6 6" /><circle cx="9" cy="9" r="2" /></svg>` }}
                                         />
-                                        <p className="text-[10px] md:text-xs text-brand-charcoal font-bold truncate w-full">{tpl.name}</p>
+                                        <p className="text-xs md:text-xs text-brand-charcoal font-bold truncate w-full">{tpl.name}</p>
                                         {tpl.id.startsWith('custom_') && (
                                             <button onClick={(e) => handleDeleteTemplate(tpl.id, e)} className="absolute -top-1 -right-1 bg-red-500 text-white rounded-full p-0.5 opacity-0 group-hover:opacity-100 transition-opacity">
                                                 <Trash2 className="w-3 h-3" />
@@ -3218,10 +3263,10 @@ export default function POSPage() {
                         </div>
 
                         {/* Content - Scrollable */}
-                        <div className="flex-1 overflow-y-auto p-4 md:p-6">
+                        <div className="flex-1 overflow-y-auto p-4 md:p-8">
                             <div className="flex flex-col lg:flex-row gap-8">
                                 {/* Left Column: Interactive Sketch */}
-                                <div className="lg:w-1/3 flex flex-col items-center justify-start bg-brand-sand/5 p-6 rounded-md border border-brand-sand/20 sticky top-0 h-fit">
+                                <div className="lg:w-1/3 flex flex-col items-center justify-start bg-brand-sand/5 p-8 rounded-md border border-brand-sand/20 sticky top-0 h-fit">
                                     {(() => {
                                         const activeTpl = hcTemplates.find(t => t.name === hcPrendaName) || hcTemplates[0] || {};
                                         const activeDetails = [];
@@ -3246,7 +3291,7 @@ export default function POSPage() {
                                                         {activeDetails.map((detail, idx) => (
                                                             <span 
                                                                 key={idx} 
-                                                                className="text-[9px] uppercase tracking-wider bg-brand-charcoal text-white px-2 py-1 rounded font-bold shadow-sm"
+                                                                className="text-[9px] uppercase tracking-wider bg-brand-charcoal text-white px-2 py-1 rounded font-bold shadow-[0_8px_30px_rgb(0,0,0,0.04)]"
                                                             >
                                                                 {detail}
                                                             </span>
@@ -3259,19 +3304,19 @@ export default function POSPage() {
                                 </div>
                                 
                                 {/* Right Column: Configuration Form */}
-                                <div className="lg:w-2/3 space-y-6">
+                                <div className="lg:w-2/3 space-y-8">
                                     {/* Garment Name */}
                                     <div>
-                                        <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1.5">Nombre de la Prenda</label>
-                                        <input type="text" value={hcPrendaName} onChange={(e) => setHcPrendaName(e.target.value)} placeholder="Ej: Vestido de noche con pedrer├¡a" className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta" />
+                                        <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1.5">Nombre de la Prenda</label>
+                                        <input type="text" value={hcPrendaName} onChange={(e) => setHcPrendaName(e.target.value)} placeholder="Ej: Vestido de noche con pedrer├¡a" className="w-full p-3 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta" />
                                     </div>
 
                             {/* Technical Parameters Grid */}
                             <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                                 {/* Molder├¡a */}
                                 <div>
-                                    <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1.5">Tipo de Molder├¡a</label>
-                                    <select value={hcPatternType} onChange={(e) => setHcPatternType(e.target.value)} className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta">
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1.5">Tipo de Molder├¡a</label>
+                                    <select value={hcPatternType} onChange={(e) => setHcPatternType(e.target.value)} className="w-full h-11 px-4 text-sm font-medium bg-white border border-zinc-200/50 rounded-2xl outline-none focus:ring-2 focus:ring-zinc-100 transition-all shadow-[0_2px_10px_rgb(0,0,0,0.02)] cursor-pointer">
                                         <option value="existing">Base existente (m├ís r├ípido)</option>
                                         <option value="custom">Desde cero (patronaje propio)</option>
                                         <option value="draping">Drapeado sobre maniqu├¡</option>
@@ -3279,13 +3324,13 @@ export default function POSPage() {
                                 </div>
                                 {/* Piezas */}
                                 <div>
-                                    <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1.5">Piezas del Patr├│n: {hcPatternPieces}</label>
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1.5">Piezas del Patr├│n: {hcPatternPieces}</label>
                                     <input type="range" min="2" max="40" value={hcPatternPieces} onChange={(e) => setHcPatternPieces(Number(e.target.value))} className="w-full accent-brand-terracotta" />
                                 </div>
                                 {/* Tela */}
                                 <div>
-                                    <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1.5">Dificultad de Tela</label>
-                                    <select value={hcTextileDifficulty} onChange={(e) => setHcTextileDifficulty(e.target.value)} className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta">
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1.5">Dificultad de Tela</label>
+                                    <select value={hcTextileDifficulty} onChange={(e) => setHcTextileDifficulty(e.target.value)} className="w-full h-11 px-4 text-sm font-medium bg-white border border-zinc-200/50 rounded-2xl outline-none focus:ring-2 focus:ring-zinc-100 transition-all shadow-[0_2px_10px_rgb(0,0,0,0.02)] cursor-pointer">
                                         <option value="easy">F├ícil (algod├│n, lino)</option>
                                         <option value="medium">Media (seda sint├®tica, jersey)</option>
                                         <option value="hard">Dif├¡cil (terciopelo, seda natural)</option>
@@ -3296,7 +3341,7 @@ export default function POSPage() {
 
                             {/* Estructura Interna */}
                             <div>
-                                <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-2">Estructura Interna</label>
+                                <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-2">Estructura Interna</label>
                                 <div className="grid grid-cols-2 md:grid-cols-5 gap-2">
                                     {[
                                         { key: 'canvas', label: 'Entretela' },
@@ -3312,7 +3357,7 @@ export default function POSPage() {
                                             className={`py-3 px-2 text-xs font-bold rounded-sm border transition-all ${
                                                 (hcInternalArchitecture as any)[item.key]
                                                     ? 'bg-brand-charcoal text-white border-brand-charcoal'
-                                                    : 'bg-gray-50 text-gray-600 border-gray-200 hover:border-brand-charcoal'
+                                                    : 'bg-gray-50 text-gray-600 border-zinc-100 hover:border-brand-charcoal'
                                             }`}
                                         >
                                             {item.label}
@@ -3323,17 +3368,17 @@ export default function POSPage() {
 
                             {/* Acabados Artesanales */}
                             <div>
-                                <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-2">Acabados Artesanales</label>
+                                <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-2">Acabados Artesanales</label>
                                 <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
-                                    <button type="button" onClick={() => setHcHandcraft(prev => ({ ...prev, handHem: !prev.handHem }))} className={`py-3 px-2 text-xs font-bold rounded-sm border transition-all ${hcHandcraft.handHem ? 'bg-brand-charcoal text-white border-brand-charcoal' : 'bg-gray-50 text-gray-600 border-gray-200'}`}>Dobladillo a mano</button>
-                                    <button type="button" onClick={() => setHcHandcraft(prev => ({ ...prev, handDraping: !prev.handDraping }))} className={`py-3 px-2 text-xs font-bold rounded-sm border transition-all ${hcHandcraft.handDraping ? 'bg-brand-charcoal text-white border-brand-charcoal' : 'bg-gray-50 text-gray-600 border-gray-200'}`}>Drapeado</button>
+                                    <button type="button" onClick={() => setHcHandcraft(prev => ({ ...prev, handHem: !prev.handHem }))} className={`py-3 px-2 text-xs font-bold rounded-sm border transition-all ${hcHandcraft.handHem ? 'bg-brand-charcoal text-white border-brand-charcoal' : 'bg-gray-50 text-gray-600 border-zinc-100'}`}>Dobladillo a mano</button>
+                                    <button type="button" onClick={() => setHcHandcraft(prev => ({ ...prev, handDraping: !prev.handDraping }))} className={`py-3 px-2 text-xs font-bold rounded-sm border transition-all ${hcHandcraft.handDraping ? 'bg-brand-charcoal text-white border-brand-charcoal' : 'bg-gray-50 text-gray-600 border-zinc-100'}`}>Drapeado</button>
                                     <div>
                                         <label className="block text-[9px] text-gray-400 mb-1">Ojales a mano</label>
-                                        <input type="number" min="0" max="20" value={hcHandcraft.handButtonholes} onChange={(e) => setHcHandcraft(prev => ({ ...prev, handButtonholes: Number(e.target.value) }))} className="w-full p-2.5 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta" />
+                                        <input type="number" min="0" max="20" value={hcHandcraft.handButtonholes} onChange={(e) => setHcHandcraft(prev => ({ ...prev, handButtonholes: Number(e.target.value) }))} className="w-full p-2.5 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta" />
                                     </div>
                                     <div>
                                         <label className="block text-[9px] text-gray-400 mb-1">Horas de bordado</label>
-                                        <input type="number" min="0" max="100" value={hcHandcraft.handEmbroideryHours} onChange={(e) => setHcHandcraft(prev => ({ ...prev, handEmbroideryHours: Number(e.target.value) }))} className="w-full p-2.5 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta" />
+                                        <input type="number" min="0" max="100" value={hcHandcraft.handEmbroideryHours} onChange={(e) => setHcHandcraft(prev => ({ ...prev, handEmbroideryHours: Number(e.target.value) }))} className="w-full p-2.5 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta" />
                                     </div>
                                 </div>
                             </div>
@@ -3341,29 +3386,29 @@ export default function POSPage() {
                             {/* Pruebas y Toile */}
                             <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                                 <div>
-                                    <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1.5">Sesiones de Calce: {hcFittingsCount}</label>
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1.5">Sesiones de Calce: {hcFittingsCount}</label>
                                     <input type="range" min="1" max="4" value={hcFittingsCount} onChange={(e) => setHcFittingsCount(Number(e.target.value))} className="w-full accent-brand-terracotta" />
                                 </div>
                                 <div className="flex items-end">
-                                    <button type="button" onClick={() => setHcToileNeeded(!hcToileNeeded)} className={`w-full py-3 text-xs font-bold rounded-sm border transition-all ${hcToileNeeded ? 'bg-brand-charcoal text-white border-brand-charcoal' : 'bg-gray-50 text-gray-600 border-gray-200'}`}>
+                                    <button type="button" onClick={() => setHcToileNeeded(!hcToileNeeded)} className={`w-full py-3 text-xs font-bold rounded-sm border transition-all ${hcToileNeeded ? 'bg-brand-charcoal text-white border-brand-charcoal' : 'bg-gray-50 text-gray-600 border-zinc-100'}`}>
                                         {hcToileNeeded ? 'Ô£ô Toile/Prueba Requerida' : 'Sin Toile'}
                                     </button>
                                 </div>
                                 <div>
-                                    <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1.5">Costo Materiales (CLP)</label>
-                                    <input type="number" min="0" step="5000" value={hcMaterialsCost} onChange={(e) => setHcMaterialsCost(Number(e.target.value))} className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta" />
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1.5">Costo Materiales (CLP)</label>
+                                    <input type="number" min="0" step="5000" value={hcMaterialsCost} onChange={(e) => setHcMaterialsCost(Number(e.target.value))} className="w-full p-3 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta" />
                                 </div>
                             </div>
 
                             {/* Extra Cost */}
                             <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                                 <div>
-                                    <label className="block text-[10px] uppercase tracking-widest text-gray-500 mb-1.5">Costos Extra (aviamentos, etc.)</label>
-                                    <input type="number" min="0" step="1000" value={hcExtraCost} onChange={(e) => setHcExtraCost(Number(e.target.value))} className="w-full p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta" />
+                                    <label className="block text-xs font-normal text-zinc-400 tracking-normal text-gray-500 mb-1.5">Costos Extra (aviamentos, etc.)</label>
+                                    <input type="number" min="0" step="1000" value={hcExtraCost} onChange={(e) => setHcExtraCost(Number(e.target.value))} className="w-full p-3 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta" />
                                 </div>
                                 {/* Save as template */}
                                 <div className="flex items-end gap-2">
-                                    <input type="text" value={newTemplateName} onChange={(e) => setNewTemplateName(e.target.value)} placeholder="Nombre para guardar como plantilla..." className="flex-1 p-3 text-sm bg-gray-50 border border-gray-200 rounded-sm outline-none focus:border-brand-terracotta" />
+                                    <input type="text" value={newTemplateName} onChange={(e) => setNewTemplateName(e.target.value)} placeholder="Nombre para guardar como plantilla..." className="flex-1 p-3 text-sm bg-gray-50 border border-zinc-100 rounded-sm outline-none focus:border-brand-terracotta" />
                                     <button type="button" onClick={() => handleSaveAsTemplateInline()} className="px-4 py-3 bg-brand-charcoal text-white text-xs font-bold rounded-sm hover:bg-brand-terracotta transition-colors whitespace-nowrap">
                                         <Sparkles className="w-4 h-4 inline mr-1" /> Guardar
                                     </button>
@@ -3373,7 +3418,7 @@ export default function POSPage() {
                             {/* AI Justification */}
                             {aiAnalysisResult?.justificacion && (
                                 <div className="bg-brand-sand/10 border border-brand-sand/30 rounded-sm p-4">
-                                    <p className="text-[10px] uppercase tracking-widest text-brand-terracotta font-bold mb-1">Justificaci├│n IA</p>
+                                    <p className="text-xs font-normal text-zinc-400 tracking-normal text-brand-terracotta font-bold mb-1">Justificaci├│n IA</p>
                                     <p className="text-xs text-gray-700 leading-relaxed">{aiAnalysisResult.justificacion}</p>
                                 </div>
                             )}
@@ -3382,9 +3427,9 @@ export default function POSPage() {
                         </div>
 
                         {/* Footer - Fixed with totals */}
-                        <div className="p-4 md:p-6 border-t border-gray-100 bg-brand-sand/5 shrink-0">
+                        <div className="p-4 md:p-8 border-t border-gray-100 bg-brand-sand/5 shrink-0">
                             <div className="flex flex-col md:flex-row items-center justify-between gap-4">
-                                <div className="flex gap-6 text-center md:text-left">
+                                <div className="flex gap-8 text-center md:text-left">
                                     <div>
                                         <p className="text-[9px] uppercase tracking-widest text-gray-400">Horas Estimadas</p>
                                         <p className="text-2xl font-bold text-brand-charcoal">{hcComputedHours}h</p>
@@ -3399,7 +3444,7 @@ export default function POSPage() {
                                     </div>
                                 </div>
                                 <div className="flex gap-3 w-full md:w-auto">
-                                    <button type="button" onClick={() => setIsHauteCoutureModalOpen(false)} className="flex-1 md:flex-none px-6 py-3 border border-gray-200 text-gray-600 text-[10px] uppercase tracking-widest font-bold hover:bg-gray-50 transition-all rounded-sm">
+                                    <button type="button" onClick={() => setIsHauteCoutureModalOpen(false)} className="flex-1 md:flex-none px-6 py-3 border border-zinc-100 text-gray-600 text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-gray-50 transition-all rounded-sm">
                                         Cancelar
                                     </button>
                                     <button
@@ -3411,7 +3456,7 @@ export default function POSPage() {
                                             if (hcPrendaName && !customOrderName) setCustomOrderName(hcPrendaName);
                                             setIsHauteCoutureModalOpen(false);
                                         }}
-                                        className="flex-1 md:flex-none px-6 py-3 bg-brand-terracotta text-white text-[10px] uppercase tracking-widest font-bold hover:bg-brand-charcoal transition-all rounded-sm shadow-md"
+                                        className="flex-1 md:flex-none px-6 py-3 bg-brand-terracotta text-white text-xs font-normal text-zinc-400 tracking-normal font-bold hover:bg-brand-charcoal transition-all rounded-sm shadow-md"
                                     >
                                         Aplicar al Pedido
                                     </button>
@@ -3428,8 +3473,43 @@ export default function POSPage() {
 
 // Helper component for polling so we can use useEffect cleanly inside the conditionally rendered block
 function PollingComponent({ checkoutResult, paymentConfirmed, setPaymentConfirmed }: any) {
+    const [emailSent, setEmailSent] = useState(false);
+
+    useEffect(() => {
+        // Enviar correo autom├ítico en cuanto se confirme el pago (ya sea por webhook o manual)
+        if (paymentConfirmed && checkoutResult && !emailSent) {
+            const isDigitalTerminal = checkoutResult?.method === 'mercadopago_point' || checkoutResult?.method?.startsWith('Mixto');
+            
+            if (isDigitalTerminal && (checkoutResult.customer?.email || checkoutResult.customer?.phone) && checkoutResult.posMode === 'new_sale') {
+                setEmailSent(true);
+                sendOrderConfirmationEmailAction({
+                    customerEmail: checkoutResult.customer.email || '',
+                    customerName: checkoutResult.customer.full_name,
+                    customerPhone: checkoutResult.customer.phone,
+                    orderId: checkoutResult.orderId,
+                    items: checkoutResult.items.map((item: any) => ({
+                        name: item.name,
+                        price: item.price,
+                        category: item.category,
+                        notes: item.notes || '',
+                        images: item.images || []
+                    })),
+                    total: checkoutResult.total,
+                    paidAmount: checkoutResult.paidAmount || checkoutResult.total,
+                    paymentMethod: checkoutResult.method || '',
+                    date: checkoutResult.date,
+                    deliveryDate: checkoutResult.deliveryDate || '',
+                    deliveryWindowStart: checkoutResult.deliveryWindowStart || '15:00',
+                    deliveryWindowEnd: checkoutResult.deliveryWindowEnd || '18:00',
+                    paymentUrl: checkoutResult.paymentUrl
+                }).catch(err => console.error('Error enviando email post-confirmaci├│n:', err));
+            }
+        }
+    }, [paymentConfirmed, checkoutResult, emailSent]);
+
     useEffect(() => {
-        if (checkoutResult?.method !== 'mercadopago_point' || paymentConfirmed) return;
+        const isDigitalTerminal = checkoutResult?.method === 'mercadopago_point' || checkoutResult?.method?.startsWith('Mixto');
+        if (!isDigitalTerminal || paymentConfirmed) return;
         
         let interval: NodeJS.Timeout;
         
diff --git a/src/app/api/webhooks/mercadopago/route.ts b/src/app/api/webhooks/mercadopago/route.ts
index 5c61113..3d66b45 100644
--- a/src/app/api/webhooks/mercadopago/route.ts
+++ b/src/app/api/webhooks/mercadopago/route.ts
@@ -25,17 +25,27 @@ async function updateDatabaseAndNotify(
     console.log(`Procesando pago aprobado para external_reference: ${externalRef}`);
     await logSystemEvent(supabase, 'INFO', `Procesando pago aprobado MP`, { paymentId, externalRef });
 
-    // Check idempotency with external_transaction_id in sales_ledger
+    // Check idempotency with external_transaction_id in sales_ledger (using the specific externalRef, e.g. the balance row)
     const { data: existingLedger } = await supabase
         .from('sales_ledger')
         .select('external_transaction_id, paid_amount, total_amount')
         .eq('internal_id', externalRef)
         .single();
         
+    const isBalance = externalRef.includes('_balance_');
+    const baseOrderId = isBalance ? externalRef.split('_balance_')[0] : externalRef;
+
+    // Get base ledger and orders using baseOrderId
+    const { data: baseLedger } = await supabase
+        .from('sales_ledger')
+        .select('paid_amount, total_amount')
+        .eq('internal_id', baseOrderId)
+        .single();
+
     const { data: existingOrders } = await supabase
         .from('production_orders')
-        .select('payment_status, payment_method, paid_amount, total_price')
-        .eq('pos_order_id', externalRef)
+        .select('payment_status, payment_method, paid_amount')
+        .eq('pos_order_id', baseOrderId)
         .limit(1);
 
     if (existingLedger && existingLedger.external_transaction_id === paymentId) {
@@ -52,9 +62,9 @@ async function updateDatabaseAndNotify(
 
     const safeAmount = Number(amount || 0);
 
-    // 1. Actualizar todas las filas de la orden en producci├│n
+    // 1. Actualizar todas las filas de la orden en producci├│n (using baseOrderId)
     const newProdPaidAmount = Number(existingOrders?.[0]?.paid_amount || 0) + safeAmount;
-    const isFullyPaidProd = newProdPaidAmount >= Number(existingOrders?.[0]?.total_price || existingLedger?.total_amount || 0);
+    const isFullyPaidProd = newProdPaidAmount >= Number(existingOrders?.[0]?.total_price || baseLedger?.total_amount || 0);
     const newProdStatus = isFullyPaidProd ? 'PAGADO' : 'partial';
 
     const { error } = await supabase
@@ -64,47 +74,39 @@ async function updateDatabaseAndNotify(
             payment_method: finalPaymentMethodLabel,
             paid_amount: newProdPaidAmount
         })
-        .eq('pos_order_id', externalRef);
+        .eq('pos_order_id', baseOrderId);
         
     if (error) {
         console.error('Error actualizando estado en BD:', error);
-        await logSystemEvent(supabase, 'ERROR', 'Error actualizando production_orders', { error, externalRef });
+        await logSystemEvent(supabase, 'ERROR', 'Error actualizando production_orders', { error, baseOrderId });
         return false;
     }
 
     console.log(`Ordenes actualizadas correctamente a ${newProdStatus} con nuevo monto pagado ${newProdPaidAmount}`);
     
-    // 2. Actualizar el registro de ventas (sales_ledger)
-    const newLedgerPaidAmount = Number(existingLedger?.paid_amount || 0) + safeAmount;
-    const isFullyPaidLedger = newLedgerPaidAmount >= Number(existingLedger?.total_amount || 0);
+    // 2. Actualizar el registro de ventas base (sales_ledger)
+    const newLedgerPaidAmount = Number(baseLedger?.paid_amount || 0) + safeAmount;
+    const isFullyPaidLedger = newLedgerPaidAmount >= Number(baseLedger?.total_amount || 0);
     
     await supabase
         .from('sales_ledger')
         .update({ 
             status: isFullyPaidLedger ? 'completed' : 'partial',
             paid_amount: newLedgerPaidAmount,
-            external_transaction_id: paymentId,
+            external_transaction_id: isBalance ? undefined : paymentId,
             payment_method: finalPaymentMethodLabel
         })
-        .eq('internal_id', externalRef);
+        .eq('internal_id', baseOrderId);
 
-    // 3. Update pending balance row if exists (para pagos de saldo con maquina)
-    const { data: pendingBalanceRow } = await supabase
-        .from('sales_ledger')
-        .select('id')
-        .like('internal_id', `${externalRef}_balance_%`)
-        .eq('status', 'pending')
-        .order('created_at', { ascending: false })
-        .limit(1)
-        .single();
-        
-    if (pendingBalanceRow) {
+    // 3. Update the specific row if it is a balance or mixed payment
+    if (isBalance) {
         await supabase.from('sales_ledger')
             .update({
                 status: 'completed',
                 paid_amount: safeAmount,
-                external_transaction_id: paymentId
-            }).eq('id', pendingBalanceRow.id);
+                external_transaction_id: paymentId,
+                payment_method: finalPaymentMethodLabel
+            }).eq('internal_id', externalRef);
     }
 
     // 3. Notificaciones autom├íticas por WhatsApp
@@ -141,13 +143,13 @@ async function updateDatabaseAndNotify(
         // Obtener detalles de la orden para personalizar el mensaje
         const { data: orders } = await supabase
             .from('production_orders')
-            .select('customer_name, customer_phone, item_name, total_price')
-            .eq('pos_order_id', externalRef)
+            .select('customer_name, customer_phone, item_name')
+            .eq('pos_order_id', baseOrderId)
             .limit(1);
 
         if (orders && orders.length > 0) {
             const order = orders[0];
-            const finalAmount = amount || order.total_price || 0;
+            const finalAmount = amount || baseLedger?.total_amount || 0;
             const monto = new Intl.NumberFormat('es-CL', { style: 'currency', currency: 'CLP', maximumFractionDigits: 0 }).format(finalAmount);
             const prenda = order.item_name || 'Servicio';
             const clienteName = order.customer_name || 'Clienta';
@@ -327,8 +329,35 @@ export async function POST(req: Request) {
                             console.warn('El pago aprobado no tiene external_reference. Imposible asociar autom├íticamente.');
                             await logSystemEvent(supabase, 'WARN', 'Pago aprobado sin external_reference', { paymentId });
                         }
+                    } else if (payment.status === 'rejected' || payment.status === 'cancelled') {
+                        const externalRef = payment.external_reference;
+                        if (externalRef) {
+                            await logSystemEvent(supabase, 'WARN', `Pago rechazado/cancelado. Limpiando orden ${externalRef}`, { paymentId, status: payment.status });
+                            const isBalance = externalRef.includes('_balance_');
+                            if (isBalance) {
+                                const { data: pendingBalanceRows } = await supabase
+                                    .from('sales_ledger')
+                                    .select('*')
+                                    .like('internal_id', `${externalRef}_balance_%`)
+                                    .eq('status', 'pending');
+                                if (pendingBalanceRows && pendingBalanceRows.length > 0) {
+                                    for (const row of pendingBalanceRows) {
+                                        await supabase.from('sales_ledger').delete().eq('id', row.id);
+                                        await supabase.from('cash_movements').delete().like('reason', `%${row.internal_id}%`);
+                                    }
+                                }
+                                await supabase.from('production_orders').update({ payment_status: 'partial' }).eq('pos_order_id', externalRef);
+                                await supabase.from('sales_ledger').update({ status: 'partial' }).eq('internal_id', externalRef);
+                            } else {
+                                await supabase.from('production_orders').delete().eq('pos_order_id', externalRef);
+                                await supabase.from('sales_ledger').delete().eq('internal_id', externalRef);
+                                await supabase.from('cash_movements').delete().like('reason', `%${externalRef}%`);
+                            }
+                        } else {
+                            await logSystemEvent(supabase, 'WARN', `Pago rechazado pero sin external_reference`, { paymentId, status: payment.status });
+                        }
                     } else {
-                        await logSystemEvent(supabase, 'INFO', `Pago no est├í aprobado. Estado: ${payment.status}`, { paymentId, status: payment.status });
+                        await logSystemEvent(supabase, 'INFO', `Pago no est├í aprobado ni rechazado. Estado: ${payment.status}`, { paymentId, status: payment.status });
                     }
                 } else {
                     const errorText = await mpResponse.text();
